"""API routers: one module per CLAUDE.md section-12 group; ``stubs`` holds the contract-only
endpoints that answer 501 until their engine's phase lands."""

from __future__ import annotations

__all__ = ["city", "cycle", "health", "live", "replay", "runs", "stubs"]
