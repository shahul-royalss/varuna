# Simplifications versus the blueprint

Every simplification of the blueprint (`docs/VARUNA_SIH2026_Blueprint.pdf`) that the prototype makes.
Where a user can see the effect, the UI carries the matching honesty label.

| Area | Blueprint | Prototype | UI label | Upgrade path |
|---|---|---|---|---|
| Radar input | Raw IMD DWR volumes or decoded PNGs | Storm-designer reconstruction calibrated to public gauge totals | "Reconstructed replay" | `services/sky/decode_imd.py` (P1), NetCDF reader |
| Drain graph | Municipal SWD GIS or SWMM model | Inferred from roads, DEM and design norms (blueprint section 7.1) | "Inferred drain graph" | SWMM `.inp` import |
| 1D hydraulics | Dynamic wave (SWMM5 via PySWMM) | Head-driven Manning "diffusive-wave-lite" with surcharge, backflow and a Preissmann slot | listed on `/verify` limitations | PySWMM adapter behind the same `Drain1D` interface (P1) |
| 2D solver | GPU local-inertial at 10 m plus 5 m nests | CPU Numba local-inertial at 30 m; nests P1 | run stamp shows the grid | Numba-CUDA or Rust kernels |
| Surrogate | GNN (MeshGraphNets family); CLAUDE.md 11.7 specifies the P0 stand-in as a two-reservoir Nash cascade **per surface unit**, coupled through `drain1d`, fitted to at least 200 Twin runs | Reduced-order emulator fitted **per road segment**, to the *deviation from a base state*, with no `drain1d` anywhere in the fit (ADR-0025). Two reasons, both measured: every product downstream is keyed on road segments, so a unit-keyed fit would predict a target it never serves; and a local-rain cascade cannot reproduce depth that arrives as tide and upstream routing - fitting the full field gave CSI 0.02 at 30 cm. Fitted to **8** Twin runs (6 fitted, 2 held out), not 200, because each is three minutes of CPU. Held-out skill is published rather than asserted: RMSE 5.704 cm, CSI 0.0854 at 30 cm | "Reduced-order emulator calibrated to VARUNA-Twin", with the held-out skill printed beside every what-if answer | PyTorch Geometric surrogate behind `FlashModel` (P7.12, P1) |
| Gauge merge | MFB plus kriging with external drift | MFB plus inverse-distance residuals | none visible | pykrige KED (P1) |
| NWP blend | Exponential-decay blend with NCUM or GFS | Disabled (no NWP in P0), labelled | "no NWP blend" on `/verify` | config switch |
| Traffic feed | Mappls or TomTom | Synthetic baseline plus anomalies plus 3 % confounders | "Synthetic traffic" | provider adapter (P2) |
| Citizen reports | App or WhatsApp with CV depth | Synthetic stream plus real sourced ground-truth pins | "synthetic" flag per report | CV depth (V10) |
| Pump inventory | Municipal fleet | 12 synthetic pumps at plausible depots | "Synthetic pump inventory" | municipal list |
| Pump optimiser | MILP (OR-Tools) | Greedy by capacity | none visible | OR-Tools MILP (P1) |
| Routing | Rust Axum with contraction hierarchies | Python time-dependent Dijkstra (networkx) | none visible | Rust service, same contract (P2) |
| Alerts | CAP into Sachet, real WhatsApp | CAP 1.2 XML (status Exercise on replay) plus an on-screen phone mock | "Exercise" status, phone mock | Twilio or WhatsApp Cloud keys |
| Storage | PostGIS, Timescale, MinIO, Redis | Files under `data/runs/` plus an in-process asyncio bus | none visible | `sinks/postgis.py` (P1) |
| DEM | LiDAR in the pilot | Copernicus GLO-30 | limitations text | drone LiDAR |
| Spurious-pit breaching | Priority-flood breaching of pits below 900 m2 | Applied inclusively: a pit no bigger than one 30 m cell (900 m2) is breached as a DEM artefact, because at this resolution a single-cell pit is the resolution limit rather than a hydrological feature (ADR-0019). The Mumbai build breaches 1,498 of 4,004 and keeps 2,493 as larger; the chronic register and underpasses override area, so the one-cell sinks that are real are kept | `city/<city>/REPORT.md` states the rule, the method and all three counts | 5 m nests (P1), where a cell is 25 m2 and the threshold starts selecting on hydrology instead |
| City map layers | Vector tiles | Simplified WGS84 GeoJSON per layer (2 m tolerance, trimmed properties, gzip on the wire: 18 MB of drains becomes 1.3 MB) | none visible | PMTiles basemap and tiled layers (P1) |
| Chronic register coordinates | Every point surveyed | 27 of 28 points geocoded against OSM or Nominatim; the one that could not be pinned (Khar subway) is kept because it is sourced, and carries `coord_verified: false` | `coord_verified` on the feature | field survey |
| Replay window accumulation | Gauge hyetograph for the event window | Inferred, not measured: no hourly or three-hourly hyetograph exists for any Mumbai station over 05:40-09:40 IST on 2 July 2019, so the storm designer is scaled from the IMD Santacruz 24-hour total (375.2 mm to 08:30) with the 183 mm/3 h anchor and the 63 mm/6 h anchor from the night before the event night (23:30 IST 30 June to 05:30 IST 1 July) as context. `calibrate()` reports what it achieved and `manifest.calibration_basis` states the reasoning verbatim | "Reconstructed replay"; the basis text on `/replay` | IMD RMC Mumbai daily weather report archive, or a MoES data request |
| Design storms | 25-year intensity from a fitted IDF curve | Chicago hyetograph built from the drainage-norm design intensity in the city config (25 mm/h legacy, 50 mm/h upgraded), with stated shape parameters. No published IDF curve for Mumbai or Chennai could be sourced (`docs/research/data_sources.md` section 6), and no depth-duration ratio was invented; "25yr" in the bundle id is a name, not a fitted return period | `design_storm.basis` on every design-storm screen | CPHEEO Manual on Storm Water Drainage Systems (2019) IDF tables, IMD's short-duration IDF atlas, or a published Gumbel/GEV fit |
| Rain-gauge network | Municipal and IMD automatic weather stations reporting live | Synthetic readings sampled from the reconstructed rain field at the ten stations in `docs/research/bmc_aws_stations.json` that carry a published coordinate; the two IMD observatories come from NOAA station history, the municipal gauges are sited at the OpenStreetMap position of their host facility because the BMC AWS coordinates sit behind a POST-only endpoint | "Synthetic gauges, inferred siting" | POST `dmwebtwo.mcgm.gov.in/api/location/loadActiveAWSLocations`, or a MoES/BMC data request |
| Tide boundary series | Survey of India / INCOIS tide table for the event day | One semi-diurnal harmonic (M2) whose crest is the single sourced height - 4.92 m at 11:30 IST as stated afterwards by the Additional Municipal Commissioner - and whose trough is assumed to sit at chart datum, because no low water is sourced; the conflicting 4.59 m / 11:52 forecast is recorded and not averaged in. The rising stage inside the demo window, and the tide-locked outfall that follows, are consequences of that one anchor | "illustrative" in every `tide.csv` row and in `manifest.calibration_basis` | Survey of India / INCOIS / Mumbai Port tide tables for 2019 |
| Tide datum | A tide table in a named datum, converted to the DEM's vertical frame with a surveyed local separation between geoid and mean sea level | `tide.csv` stays in chart datum as sourced, and is only assumed to be chart datum, because the 4.92 m civic statement names none. The Twin subtracts mean sea level at **2.70 m above chart datum** (PSMSL station 43, Apollo Bandar; range 2.66-2.73 m over 2015-2024, with 2019 interpolated at 2.711 m) and treats mean sea level as the DEM's EGM2008 frame, so the demo window's stage reads -2.655 to +1.236 m rather than 0.045 to 3.936 m (ADR-0055). The residual is not quantified: any EGM2008-to-local-MSL separation, and any revision of the chart datum since PSMSL's note (its benchmark changed in 1966, and the 2020 and 2024 annual values are flagged), stays in the tidal boundary as an unmeasured bias. `varuna city audit-gravity` does not convert yet, so its tidal comparison is 2.70 m high, and every baked run predates the conversion | "illustrative" in every `tide.csv` row; the `tide.stage` event and the run notes carry the datum; no screen shows the stage | a Survey of India / INCOIS 2019 table for Apollo Bandar in a named datum, plus a measured EGM2008-to-MSL offset at Mumbai |
| Traffic anomaly footprint | Probe speeds from a provider, everywhere | The feed covers the 9,069 segments of class tertiary and above, as a probe provider does; anomalies are placed at the segment nearest each sourced pin for ten minutes either side of its time and spread two hops along the road graph, and 3 % of covered segments carry an unlabelled unrelated slowdown so Pulse's rejection logic has something to reject | "Synthetic traffic" | Mappls or TomTom adapter (P2) |
| Citizen-report depth chips | A depth read from a photo or stated by the reporter | Ankle/knee/waist chips follow a stated rule from the reconstructed rain accumulated over the hotspot (25 / 55 / 85 mm), not from a hydraulic depth; the sourced pins carried in the same stream get no chip at all, because no cached source states a depth in centimetres | "synthetic" per report; `depth_phrase` shown verbatim on sourced pins | CV depth estimation (V10); a sourced depth measurement |
| Replay stream re-publication | Every pass over an instant re-emits its data, as a broker would on replay | Each stream event and cycle fires once per clock session: seeking backwards rewinds the clock without re-publishing what the console already holds, so a cycle can never trigger twice for the same instant (CLAUDE.md 10.2, ADR-0013). `POST /v1/replay/bundle` or a fresh `rewind()` starts a new pass | the replay panel shows the clock's own note | Redpanda topics with per-consumer offsets (P2) |
| Nowcast ensemble | pySTEPS STEPS: cascade advected between every step, probability matching re-imposing the observed intensity distribution on each member | pySTEPS STEPS is what runs. The hand-rolled fallback of CLAUDE.md 17, used only when pySTEPS cannot be imported or raises, departs from it twice: the AR recursion runs in the Lagrangian frame for the whole horizon with one advection at the end (identical for the constant motion field optical flow gives us, divergent under strong shear), and there is no probability matching, so the AR(2) sets each member's intensity distribution. A level whose AR(2) fit lands on complex roots degrades to AR(1) rather than ring (ADR-0015) | `RainEnsemble.source` reads `fallback_steps`, and the run stamp shows it | pySTEPS is already the default; the fallback is insurance, not a plan |
| Rain fan chart at a junction | The rain a street receives | The fan chart at a hotspot is the 500 m Sky pixel the junction falls in, because the quantile and exceedance grids are published on the Sky grid; the 30 m resample of the ensemble is the Twin's forcing and is never written to disk (485 MB a cycle, CLAUDE.md 10.3). One pixel spans several junctions, so two neighbouring hotspots can share a rain series while their depth series differ | `point.res_m` is 500 m on every response, and the panel says the pixel it sampled | the 30 m cube, once a run can afford to store it |
| 1D drain hydraulics | Dynamic wave (SWMM5, full Saint-Venant with inertia) | Head-driven "diffusive-wave-lite": `Q = sign(dH)*min(Q_full, (1/n) A_eff R_h^(2/3) sqrt(|dH|/L))` on an explicit 1 s inner step, with capacity, fill fraction, pressurised flow, backflow, flap gates and a Preissmann slot. It carries no momentum, so it cannot represent a bore or a hydraulic jump inside a pipe, and it reaches a new steady state one or two seconds sooner than a dynamic-wave model would | `/verify` limitations | PySWMM adapter behind the same `Drain1D` interface (P4.9, P1) |
| Surface-drain exchange rate | Continuous two-way coupling, sub-stepped to convergence | Fluxes frozen for a 5 s sync interval and limited to the volume that is actually present (CLAUDE.md 11.5). The limit binds: at a 5 s interval the orifice equation `0.6 A_m sqrt(2g(H-z_g-h))` asks for about 19x more water than a surcharging manhole holds, so the exchange is volume-limited rather than orifice-limited almost everywhere. Removing the limit is measurable and wrong - it takes the coupled mass balance from 9.8e-4 to 3.6e-2 and doubles peak depths | none visible; the mass balance is on `/verify` | shorter sync, or an implicit exchange solved to convergence |
| Drain invert geometry | Surveyed inverts from a municipal SWD model | Inferred at a fixed cover below ground - 1.5 m, 3 m on trunks (CLAUDE.md 10.1 step 7) - and never deepened, so the 0.3 % minimum slope is the rule that gives way. Measured by `varuna city audit-gravity --city mumbai` (ADR-0048): 18,994 of 49,770 edges (38.2 %, 686 km) run uphill and 26,651 fall less than 0.3 %, while the stored `slope` column still reads at least 0.003 on all of them. On 27,974 of 49,897 nodes (56.1 %) some invert between the node and its outfall stands above the node's own street (median 3.17 m above, max 31.97 m), so water there reaches an outfall only by surcharging first - Hindmata's surcharge in the demo runs comes from an 8.56 m sill of this kind, not from its real tide-locked mechanism. REPORT.md's 100 % connectivity is topological; the hydraulic figure is 43.9 %. A regrade is decided and not yet built | "Inferred drain graph" | regrade within the blueprint's 1-3 m depth bound with adverse edges flagged, then a real SWD model, or LiDAR plus manhole survey |
| DEM vertical accuracy | LiDAR at 1 m, +/- 0.15 m vertical | Copernicus GLO-30 at 30 m, hydro-conditioned (buildings burned, roads carved, culverts breached). Absolute depths at a junction inherit the DEM's metre-scale vertical error, so VARUNA claims the pattern and the timing of flooding rather than a surveyed depth, and is calibrated at the chronic spots | limitations text on `/verify` | drone LiDAR in the pilot |
| What-if cleaning lever | Clean pipes, beta -> 0.05, and the junction drops (CLAUDE.md 7.2, 11.7) | Element-wise emulator: `simulate()` indexes every term by segment, so a pipe that is not under the target has **exactly zero** effect on it (71.5821 cm against a base of 71.5821 at the deepest street; 0 of Hindmata's 24 sibling candidates score anything), and desilting the **whole city** at once moves the deepest street by **3.466 cm** (mean 0.1416 cm, 2,032 of 21,296 segments over 0.5 cm) on the 08:40 cycle and 1.519 cm on the newest. So there is no ranking to read and no 35 cm drop to show (ADR-0042) | printed beside the "Clean top 14" switch, and `attribute()` refuses with the reason rather than listing zeros | `drain1d` inside the attribution loop, or the GNN surrogate (P7.12) |
| Traffic anomaly baseline | Weekday-hour mean and sigma per segment (CLAUDE.md 11.6, `z = (v - mu_wd,hr)/sigma`) | Mean is the feed's `baseline_kmh`. Sigma is not in the feed and is estimated from the segment's own scatter over the twelve snapshots (an hour of 5-minute probes) ending before the window being scored, with a `max(0.2 x baseline, 3 km/h)` floor where that estimate is missing or below 1 km/h. It used to be estimated inside the 2-snapshot persistence window, which makes the denominator `\|dv\|/sqrt(2)` rather than an estimate: **42.4 %** of rows at the 03:40Z cycle took the floor, because a segment equally slow twice has no scatter at all. Over the wider window **1.8 %** do. The window ends before the scored snapshots because a sigma that contains the anomaly is inflated by it - including them takes that cycle from 22 anomalies to 3, and the 02:40Z cycle to none; excluding them the count holds (22 -> 24) | none yet | pass sigma through the feed |
| EnKF observation operator | `H(theta)` runs `drain1d` per member with the surface inflows frozen from the last Twin run, and reads depth at the observation cells through the Flash-lite emulator (CLAUDE.md 11.6) | An analytic capacity deficit - `capacity_operator` in `services/pulse/varuna_pulse/enkf.py`, stamped into every drain-health product as `operator: "capacity_deficit"`. Over a 30-minute window the catchment delivers `area x rain`, the pipe removes `(1 - beta) Q_full`, and the remainder ponds over a fixed 1,500 m2 junction: `depth = max(0, Q_in - (1 - beta) Q_full) x window / ponding_area`. It is monotone in beta and roughly linear, which is what the Kalman gain needs, and it is not a hydraulic model: no routing, no backflow, no tide, no upper bound. Applied in turn to each of Mumbai's 49,770 pipes at 60 mm/h and beta = 0.5 it implies a median depth of 6.8 cm, a 95th percentile of **7.1 m** and a maximum of 124 m, with 13.7 % of pipes over 2 m - so on a large catchment the depth the filter compares an observation against is not a depth a street could hold, and the innovation it acts on is correspondingly large | "Observation operator: capacity deficit" in the `/drains` map panel header | `drain1d` per member behind the same `ObservationOperator` protocol, once Flash carries an edge-to-segment map (11.6, P7.12) |
| EnKF update | One ensemble Kalman update per cycle, `theta^a = theta^f + P_theta_y (P_yy + R)^-1 (y + eps - H(theta^f))`, N_e = 50 (CLAUDE.md 11.6, Appendix A) | The same perturbed-observation update applied as an ensemble smoother with multiple data assimilation: 4 passes per batch, R inflated x4, H re-evaluated each pass, from a moment-matched prior draw, N_e = 50 (`services/pulse/varuna_pulse/enkf.py`, ADR-0044). On a non-linear H this approaches the exact posterior where one linearised step cannot. On the 11.6 fixture it is within 0.017 of exact Bayes in mean and 0.027 in spread cut (median over ten ensemble seeds), against a 0.115 spread gap for the single step. The spread is the Bayes posterior's, so with a wrong prior the truth lies inside the 90 % interval only about 40-46 % of the time | "Learned blockage" beta +/- sd on `/drains`; the product's `notes` names the method | Same `assimilate()` contract; with a drain1d operator, fewer passes or an iterative smoother if 4 evaluations of H do not fit the 3 s stage |
| Street ensemble | 50 members: 20 Sky members x parameter draws from the Pulse posterior, through the emulator, giving per-segment quantiles and exceedance probabilities (CLAUDE.md 11.7) | 20 members, and no Pulse draws. The **level** is one deterministic Twin run on the Sky ensemble *mean*; the **spread** is each Sky member's deviation from the member mean, carried across from Flash-lite, so the member mean of the stack is the Twin's depth exactly (ADR-0025: the emulator's held-out RMSE is 5.7 cm, so it cannot be the number on screen). The members carry the per-member AOI-mean hyetograph - the only per-member rain a cycle keeps - so they differ in amplitude and not in where the cell sits, and the band is not the ensemble's disagreement about the map. Pulse publishes beta per drain edge while the emulator is keyed on road segments, so there is no map to carry the posterior across and every member runs at the fit's reference blockage. Where the stage cannot honestly run - a design storm, no fitted emulator for the city, no per-member hyetographs - `ensemble_n` falls back to **one deterministic** member, `p10 = p50 = p90`, and every exceedance is 0 or 1 | the run stamp reads "1 member (deterministic)" or "20 members" from `ensemble_n`, the probability legend says so when it is 1, and the run's notes name the case that applied | the edge-to-segment map, then parameter draws from the Pulse posterior in `varuna_flash.model` (P7.6) |
| Observations product | `observations.parquet` in the run directory, carrying the beta change each observation caused (CLAUDE.md 10.3, 11.6); the drain X-ray's timeline lists every observation assimilated in the current replay (7.3) | `observations.json`, written by `write_observations` in `services/pulse/varuna_pulse/cycle.py` and served as-is by `GET /v1/observations`. **Why JSON, measured on the baked 2 July runs:** a cycle assimilates 8 to 40 observations, and at that row count parquet's schema and footer make the file larger, not smaller - 8 observations are 1,859 bytes of JSON against 6,414 bytes of parquet holding the observation records alone, 39 are 8,575 against 9,794. The payload is also not one table: the disagreement list and the notes travel beside the observations, and the console reads it without a parquet decoder. **Scope, one cycle and not the replay:** the file is per run, and the two streams in it accumulate differently. Reports are read from the start of the bundle up to the cycle (`read_reports(..., until=cycle_ts)`, de-duplicated within 50 m and 10 minutes), so they grow 0, 0, 0, 2, 5, 6, 10, 21 across the eight 2 July bakes; traffic anomalies are only those detected at the cycle's own snapshot (8 to 18 per cycle), and an earlier cycle's anomaly is not carried forward. The timeline was **not** changed to accumulate across the runs the client has loaded - it loads one run - so it states its scope instead | the `/drains` timeline description: "The traffic anomalies Pulse detected at this cycle and the citizen reports filed up to it ... Earlier cycles' anomalies are not listed." | parquet beside the JSON once a cycle carries enough rows for it to pay; a replay-wide timeline read across the run registry |
| Node forecast | `node_forecast.parquet`: one row per drain node and valid time with `head_p50`, `p_surcharge`, `q_surcharge_p50` and `responsible_edges[]` (CLAUDE.md 10.3; `NodeForecastRow` in `packages/schemas/varuna_schemas/models/forecast.py`) | **Not written; no `node_forecast` file exists in any run.** Two files stand in. `node_summary.json` gives, for each of the 36 steps, how many of the 49,897 nodes surcharge and how many edges run backwards. `node_surcharge.json` keeps the 500 manholes with the largest peak surcharge discharge - position, first and last step, a per-step discharge series - and the first 500 reversed edges, each tagged tidal or not, with the true totals (`n_surcharging`, `n_reversed_edges`) reported beside them. A node record carries **no head, no P(surcharge) and no responsible edges**: the discharge is the single Twin run's, not a median over members, so there is no probability to publish, and attribution (P7.7) has not run, so no pipe is named as responsible. The shape is a size decision: a full table is 49,897 x 36 = 1,796,292 rows to say most nodes are fine, and storing every surcharging node's series produced a 17 MB JSON the console had to download before drawing anything (`MAX_NODES` in `services/products/varuna_products/surcharge.py`). This is P5.2's debt, recorded here rather than fixed | none as a label; the `/drains` map header calls its red rings "the 500 manholes that surcharge hardest in this run", so it does not claim they are all of them, but it does not print the run's true total | head and P(surcharge) per node from the street ensemble (P7.6), responsible edges from attribution (P7.7), then `node_forecast.parquet` for the nodes that surcharge |
