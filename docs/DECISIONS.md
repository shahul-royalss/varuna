# Architecture decision records

Five lines each: context, decision, alternatives, consequence, date. Newest at the bottom.

## ADR-0001 Repository lives at `C:\dev\varuna`, not inside OneDrive

- Context: the spec folder is `C:\Users\shahu\OneDrive\Documents\for sih`. The first `uv sync` there failed with "Access is denied" on a rename inside `.venv`; GNU make and several tools mishandle spaces in paths; OneDrive syncing `node_modules` and `.next` is a known source of EBUSY failures.
- Decision: build in `C:\dev\varuna` (short path, no spaces, outside OneDrive). A pointer README is left in the OneDrive folder.
- Alternatives: relocate only `.venv` via `UV_PROJECT_ENVIRONMENT` (fragile; `node_modules` and `.next` would still be in OneDrive); a directory junction (OneDrive does not sync through junctions reliably).
- Consequence: contributors clone or copy to a non-synced path; the repo is a normal git repository and can be pushed anywhere.
- Date: 2026-09-04

## ADR-0002 Python 3.12 via a uv-managed interpreter; `system-certs = true`

- Context: the machine has Python 3.14 (too new for numba and pysteps wheels) and uv's bundled TLS roots rejected GitHub and PyPI certificates on this network ("invalid peer certificate: UnknownIssuer").
- Decision: `requires-python = ">=3.12,<3.13"`, `.python-version = 3.12`, `[tool.uv] system-certs = true` so uv uses the Windows certificate store.
- Alternatives: pin 3.13 (numba and pysteps lag); set `UV_SYSTEM_CERTS` per shell (forgettable).
- Consequence: `uv sync` works from a clean clone; Python 3.12.14 is downloaded automatically by uv.
- Date: 2026-09-04

## ADR-0003 uv workspace with one package per service

- Context: the spec wants every engine as a Python package under `services/` with its own tests, CLI and `run()`.
- Decision: the root `pyproject.toml` is a uv workspace; members are `packages/schemas` and `services/*`; import names are `varuna_<service>`; the root package `varuna` (in `tools/varuna_cli`) provides the `varuna` CLI and depends on every member.
- Alternatives: a single monolithic package (blurs service boundaries); separate virtualenvs per service (slow, duplicated science stack).
- Consequence: `uv sync` installs everything editable; `uv run varuna <task>` mirrors every `make` target so Windows users without GNU make are not blocked.
- Date: 2026-09-04

## ADR-0004 GNU make from winget (ezwinports) plus a Python task runner

- Context: `make` was not installed; Chocolatey needs admin rights.
- Decision: install `ezwinports.make` via winget at user scope; implement every target in `tools/varuna_cli` so `make <target>` is a thin wrapper around `uv run varuna <target>`.
- Alternatives: `just`; npm scripts only; PowerShell scripts.
- Consequence: `make demo` works in any new terminal; `uv run varuna demo` is the identical fallback.
- Date: 2026-09-04

## ADR-0005 Run artifacts are files; PostGIS is P1

- Context: spec section 4.2.
- Decision: `data/runs/<run_id>/` holds GeoParquet, GeoJSON, PNG, Zarr and JSON served by FastAPI; an in-process asyncio bus replaces Redis and Redpanda with the same topic names.
- Alternatives: PostGIS and Timescale from day one (infrastructure on stage); SQLite.
- Consequence: zero infrastructure on stage, byte-reproducible bakes, trivial offline packaging; the PostGIS sink mirrors the blueprint schema later.
- Date: 2026-09-04

## ADR-0006 Network access goes through the Windows trust store; GDAL never reads remote files

- Context: Norton "Web/Mail Shield" re-signs every TLS connection on the demo laptop. Python `requests` (certifi) and GDAL's bundled curl fail verification ("unable to get local issuer certificate", "schannel: the certificate chain is incomplete"); `urllib` with the Windows store and Node work. Verified 2026-09-04 with `openssl s_client` (issuer "Norton Web/Mail Shield Root").
- Decision: every HTTP download in the Python services goes through `varuna_schemas.net` which calls `truststore.inject_into_ssl()` once (OS trust store on Windows, macOS and Linux) and streams to `city/cache/` with resume and size checks. Rasters are always read from the local cache; `vsicurl` is not used. `GDAL_HTTP_UNSAFESSL` is never set.
- Alternatives: export the Norton root into a PEM bundle and set `CURL_CA_BUNDLE` (machine-specific, fragile); disable certificate checks (unsafe); ask the user to disable Norton scanning (out of our hands on stage).
- Consequence: the pipeline works on this laptop and on a clean Linux CI runner unchanged; first-run downloads are explicit, cached and inspectable; the offline package ships the cache.
- Date: 2026-09-04

## ADR-0007 The demo replays 2 July 2019 at 06:40 IST, not 15:40

- Context: the curated ground truth (29 sourced pins inside the area of interest, `docs/research/ground_truth_MUM-2019-07-02.md`) shows the Mumbai cloudburst peaked overnight on 1-2 July and the civic reports cluster between 08:07 and 14:28 IST on 2 July; by 17:22 IST the municipal corporation said waterlogging had receded. The spec's demo script opened at 15:40 IST, after the event, where no pin would land.
- Decision: the replay window becomes 05:40 to 09:40 IST on 2 July 2019 with the cycle opening at 06:40 IST. Thirteen sourced pins then land inside the forecast window at lead times of 87 to 150 minutes, so every pin arrives between one and a half and two and a half hours after VARUNA flags the street.
- Alternatives: open at 08:00 (fourteen pins, but some land only 7 minutes ahead, which demonstrates nowcasting the present rather than forecasting); keep 15:40 and accept that no pin lands (breaks rule 7 and the 2:40 beat of the demo); switch to another Mumbai event (unnecessary, this one yields 29 pins against a minimum of 10).
- Consequence: the demo script times in section 15 shift by nine hours; the storm designer calibrates to the 05:40-09:40 window, which **straddles the 08:30 edge of** the one primary-sourced total (Santacruz 375.2 mm for the 24 hours ending 08:30 IST on 2 July, IMD) - 170 of its 240 minutes are inside that total and the last 70 are in the next day's, which `manifest.calibration_basis` now states with the split measured off the cube (corrected 2026-09-12; this line read "sits inside", which is what the overlap disclosure exists to stop a reader assuming); the "three hours early" claim is now evidenced rather than asserted. Depth MAE cannot be scored for this event because no cached source states a depth in centimetres, so `/verify` reports occurrence, place and timing only, and says why.
- Date: 2026-09-06

## ADR-0008 The city build is a chain of cache-and-reload steps, in dependency order

- Context: `make city` runs eleven steps of CLAUDE.md 10.1 over 168,606 cells and 34,539 road edges; a full build is minutes, and Phase 2 onwards will re-run it constantly while iterating on one step.
- Decision: every step in `varuna_city.pipeline` declares the files it writes and the files it reads, and carries both a `build` and a `load`; when its outputs are newer than its inputs it *reloads* its products from `city/<city>/` instead of recomputing them. Steps run in dependency order, not the spec's numbering: hotspots before conditioning (chronic underpasses must survive as sinks), drains before units (a unit is the watershed draining to an inlet), and the units are joined back onto the drain nodes so `inlet_links.parquet` names real units.
- Alternatives: recompute everything every time (a 5-minute inner loop); a build system such as doit or snakemake (another dependency and another language on stage); hand-rolled `if path.exists()` checks inside each module (the staleness rule then lives in twelve places).
- Consequence: a cold build of Mumbai from the open-data cache is 2 minutes 43 seconds and a warm re-run is 15 seconds; `--only <step>` rebuilds one step and lets the rest load; every step's numbers land in `pipeline.json` and in `REPORT.md` whether it ran or reloaded.
- Date: 2026-09-07

## ADR-0009 At 30 m the spurious-pit rule cannot fire, and that is reported rather than tuned

> **Superseded by ADR-0019 (2026-09-10).** The premise below is wrong, and reading it as current
> state will mislead. It treated `area >= min_area_m2` as the spec's rule and concluded the rule
> could never fire; that comparison was a defect, not a property of the grid. The threshold means
> "a pit no bigger than one cell", so it is inclusive: the build now breaches 1,498 spurious pits
> of 4,004 and keeps 2,493 because they are larger. Kept for the record of what was believed.

- Context: CLAUDE.md 10.1 step 4 breaches depressions smaller than 900 m2 as DEM artefacts. One cell of the 30 m city grid is exactly 900 m2, so no pit on this grid is ever smaller than the threshold: the Mumbai build finds 4,004 pits and breaches none of them as spurious.
- Decision: leave the rule and the threshold as the spec states them, and print the arithmetic in `city/<city>/REPORT.md` next to the number. Buildings are still burned, roads carved and 1,041 culverts and bridges breached; every remaining pit is kept and reported, noise included.
- Alternatives: raise the threshold to a few cells (invents a filter the spec did not ask for and silently deletes real chronic dips); drop the rule (loses it for the 5 m nests); tune until the number looks busy (a fabricated result).
- Consequence: the depression map at 30 m is the pit set of the conditioned DEM as it stands; the rule starts selecting on the 5 m nests (25 m2 cells) of CLAUDE.md 3.3, and the report says so where a judge will read it. Depressions whose bottom cell is permanent water are dropped instead (58 of 4,004): they are the bay and the creek, not a street.
- Date: 2026-09-07

## ADR-0010 Lane counts take the maximum of an OpenStreetMap tag collection

- Context: `make city` was not reproducible. Two cold builds of Mumbai agreed on every geometry and identifier but disagreed on `lanes` for 4 of 21,296 road segments. OSMnx returns a collection of tag values whenever it simplifies several ways into one edge, and that collection is sometimes a `set`, whose iteration order changes with Python's per-process string hash seed; the helper took the first item it saw.
- Decision: collect every integer in the value and take the maximum. The result is order-independent, and where a simplified edge spans a widening the widest cross-section is the one that carries the traffic the exposure weight represents.
- Alternatives: take the minimum (defensible as a bottleneck, but exposure weighting is about how much traffic the street carries); sort and take the first (arbitrary); set `PYTHONHASHSEED` (hides the bug rather than fixing it, and only inside our own processes).
- Consequence: the segment table is now identical across runs. A regression test in `services/city/tests/test_segments.py` pins the behaviour for lists, sets and scalars. One source of non-reproducibility remains, float noise in `pyflwdir.fill_depressions`, recorded in `city/mumbai/REPORT.md`.
- Date: 2026-09-07

## ADR-0011 Ruff exclusions are anchored to the repository root

- Context: `uv run ruff check .` reported "All checks passed" while `uv run ruff check services/city` found 30 errors. The root configuration excluded `"city"`, and an unanchored pattern matches a directory of that name anywhere in the tree, so the entire city service was silently unlinted from the day it was written.
- Decision: anchor the data-directory exclusions as `/city`, `/data` and `/bundles` so they match only the gitignored folders at the repository root.
- Alternatives: rename the service (churn, and the spec names it `services/city`); list the service explicitly in `include` (fights the exclusion rather than fixing it).
- Consequence: the city service is linted; the 30 findings it had were fixed. Any future service under a name that collides with a data directory is covered automatically.
- Date: 2026-09-07

## ADR-0012 The demo window carries one fifth of the sourced daily rainfall total, and the storm is aimed at the pins

- Context: the only IMD-primary rainfall number for the event is 375.2 mm at Santacruz for the 24 hours ending 08:30 IST on 2 July 2019, read off IMD Mumbai's own chart. No hourly or three-hourly hyetograph exists for any Mumbai station over the replay window, and the nearest sub-daily anchor (Skymet's 63 mm in the 6 hours to 05:30 IST on 1 July, the night before the event night) ends a day and four hours before the window opens. Something has to set the depth of the storm the console shows, and whatever sets it is an inference.
- Decision: the storm designer is calibrated to 20 % of the daily total, 0.20 x 375.2 = 75.0 mm of area-mean accumulation over MUM-CENTRAL for 05:40-09:40 IST, and its eight cells are aimed at the three latitudinal clusters of the 29 sourced ground-truth pins in proportion to the pins in each. `manifest.calibration` carries every number and `manifest.calibration_basis` carries the arithmetic and the sources, so a judge can argue with the share rather than with a black box.
- Alternatives: the uniform share (16.7 %, 62.5 mm), which ignores that the flooding started at 08:07 and worsened until 14:28; extrapolation of the Skymet rate from the night before (11.2 %, 42.0 mm), which the same record contradicts; the heaviest documented burst rate (183 mm/3 h, which would put 244 mm in the window, 65 % of the day, leaving no room for the overnight cloudburst the total is dominated by); or stating no accumulation at all, which would leave the demo with no storm.
- Consequence: the achieved accumulation is 75.0 mm, 0.0 % from target and inside the stated 5 % tolerance, with cell peaks of 44 to 83 mm/h (inside the designer's 40-120 mm/h range) and accumulation over the pins running 1.16 times the area mean. The number must never be spoken as a measurement; the manifest, the bundle card and `/replay` all say "inferred, not measured", and the same 20 % share is what would be revised first if IMD's RMC Mumbai daily weather reports for 1-3 July 2019 were obtained.
- Date: 2026-09-07

## ADR-0013 The replay clock lives in the API, and each pass publishes a bundle event once

- Context: the console, the replay page, the mode banner and every other tab must agree on one simulated instant, and P2.7 needs play, pause, seek and speed to mean the same thing everywhere. A clock in the browser would drift from the one the cycle orchestrator triggers off, and two open tabs would disagree.
- Decision: `varuna_replay.clock.ReplayClock` is the only clock. The API process holds exactly one (`varuna_api.replay.ReplayController`), the routes under `/v1/replay` drive it, and every change is published on `replay.clock`, which the WebSocket relays; the console's store follows those events instead of running a timer. Simulated time is computed from a monotonic anchor (`anchor_sim + (monotonic() - anchor_mono) * speed`), never accumulated, so it cannot drift. Each stream event and each cycle fires at most once per pass: seeking backwards rewinds the clock without re-publishing, and `rewind()` is what clears the fired sets. Pressing Play at 06:40 of a window that opens at 05:40 triggers the 06:40 cycle only, not the twelve behind it.
- Alternatives: a clock per browser tab (drifts, and the tabs disagree); re-publishing everything a backwards seek passes (would re-trigger cycles the spec says must fire once, and would swap the map a dozen times); accumulating deltas per tick (drifts by the poll error, several seconds over a four-hour replay at 30x).
- Consequence: three additions to the contract, all backward compatible: `ReplayClock.note` carries what the clock wants the operator to read ("No baked run for 06:45 IST. Run make bake ..."), `ReplayBundleSummary.built` and `missing_members` say whether a folder is more than a manifest, and `POST /v1/replay/bundle` points the clock at another bundle, which is what a card on `/replay` does. Baked mode publishes the pre-computed run for each cycle; until Phase 5 bakes any, it publishes nothing and the note says which make target fixes that. Large streams (radar frames, traffic snapshots) travel on the bus as a pointer to the bundle member plus a count, small ones (gauges, tide, reports) inline, because the bus is an event bus and Sky and Pulse read the cubes from disk.
- Date: 2026-09-07
## ADR-0014 The console deploys to Vercel and the API to Railway; Supabase is reserved for the P1 sink

- Context: the repository needed a public home and a shareable deployment. The choice offered was Railway or Supabase for the backend. VARUNA's backend is not a database with an API over it: it is a Python process carrying numba, GDAL, geopandas, pysteps and zarr, running a shallow-water solver for seconds per cycle, holding `WS /v1/live` open, and writing run artifacts to disk. Spec section 4.2 already fixed the P0 store as files under `data/runs/`, chosen for zero infrastructure on stage and offline packaging.
- Decision: Vercel hosts `apps/command`; Railway runs the API from a Dockerfile with a volume at `/data`; Supabase is reserved for the P1 PostGIS and Timescale sink in `services/api/sinks/postgis.py`, which mirrors the blueprint's section 9.1 tables.
- Alternatives: Supabase as the backend (its Deno edge functions cannot run a Numba kernel and its managed Postgres cannot host GDAL, so the engines would have nowhere to live); a virtual machine (more control, more to maintain, and nothing here needs it); no deployment at all (the finale does not need one, but mentors and the idea submission do).
- Consequence: `Dockerfile`, `docker/entrypoint.sh`, `railway.json`, `vercel.json` and `docs/DEPLOY.md` exist. Generated city layers are not committed, so the entrypoint builds them into the volume on first boot and the API starts either way, answering 404 with the command that fixes it. The deployment is a convenience: rule 15 and section 5 keep the finale offline on the laptop from `make pack`.
- Date: 2026-09-07


## ADR-0015 The fallback nowcaster degrades to AR(1) rather than ring, and both nowcasters are capped in rain space

- Context: `fallback_steps` fits AR(2) per cascade level by Yule-Walker on the lag-1 and lag-2 correlations of the Lagrangian-frame history. When the motion field does not align the frames - a zero field over a history in which the storm plainly moved, which is exactly what optical flow returns when it cannot track - the lag-2 correlation collapses below `r1^2` and the fit lands on complex characteristic roots of modulus about 0.99. That is a barely damped ringing, and because the cascade is reconstructed in dBR and comes back through an exponential, six steps of it reached 10,589 mm/h from a 31 mm/h analysis. The existing guard checked stationarity, which this fit satisfies; stationary and non-oscillatory are different conditions.
- Decision: two guards, at different depths. The fit rejects complex roots - `phi1^2 + 4 phi2 < 0` - and degrades that level to AR(1) on the lag-1 correlation alone, because a collapsed lag-2 correlation means the history cannot support a second-order model, not that the rain oscillates. Separately, `motion.MAX_RAIN_MM_H` caps every nowcast cube, pySTEPS' included, at 400 mm/h in rain space.
- Alternatives: guard on the sign of `phi2` (wrong - pySTEPS' own level-1 fit on the test storm is `phi1 = 1.999, phi2 = -0.999`, a double root at 1.0, which is slow decay and exactly what a persistent large scale should do); clamp in dBR (makes the cap a statement about the transform rather than about rainfall); clamp only the output and leave the ringing (the members would be garbage under the cap, and the ensemble spread would be meaningless); refuse to nowcast when the fit is ill-conditioned (the cycle must degrade, not stall - CLAUDE.md 11.11).
- Consequence: the pathological case now fits AR(1) at `phi1` of 0.86 to 0.97 and produces 32.2 mm/h against the 31 mm/h that went in, so the guard fixes the fit rather than leaning on the cap; the cap never binds on any replay bundle and exists only so no ill-conditioned run can publish an impossible number (rule 6). `test_frames_the_motion_field_does_not_explain_still_give_physical_rain` pins both nowcasters to this, and asserts the result stays well inside the ceiling rather than merely under it.
- Date: 2026-09-09

## ADR-0016 Rain is served under `/v1/nowcast`, and a cycle computed on demand carries no run id

- Context: task P3.8 needs the console to read Sky's products - the per-member AOI hyetographs behind the time bar's spread band, and the quantiles and exceedance at a named junction behind the fan chart - out of `rain/quantiles.zarr`, which a browser cannot open. Two things were unsettled: which URL they belong at, given that CLAUDE.md 12 names `/v1/nowcast/*` for street depth and says nothing about rain; and what `run_id` should say when Phase 5 has not baked a single run yet, so the only way to get a forecast on screen is to compute one.
- Decision: `GET /v1/nowcast/rain` (the band) and `GET /v1/nowcast/rain/series` (the fan chart at a point), inside the nowcast namespace and mirroring the `segments` / `segments/{id}/series` pair the table already names. `?compute=true` runs one Sky cycle from the replay bundle through `varuna_cycle.sky_cycle` and returns it without writing anything to `data/runs`; that response carries `run_id: null` and `mode: "live"`, and the `run_id` field is nullable for exactly this case. With no run and no `compute`, the answer is a 404 naming `make bake BUNDLE=...`.
- Alternatives: a `/v1/sky/*` namespace (invents a URL shape the contract does not have, and names the engine rather than the product); minting a run id like `MUM-...-sky1.0-twin1.0-flash0.3-live` for the computed cycle (would claim Twin and Flash versions for engines that did not run, and put an identifier on screen that resolves to nothing under `GET /v1/runs/{id}`); writing the computed cycle into `data/runs` as a real run (a directory with rain and no depth, segments, alerts or mass balance is not a run, and the registry would list it as one); returning an empty series when nothing is baked (on a chart, indistinguishable from a forecast of no rain).
- Consequence: the demo path works before `make bake` exists, and the run stamp can say "live" honestly. Computed cycles are cached three deep on (bundle folder, radar-cube mtime, cycle instant), so the band and the fan chart cost one cycle between them and a rebuilt bundle never serves a stale one; the requested time is floored onto the bundle's five-minute cycle ladder and clamped to the first instant with three radar frames behind it, and the response's `valid_ts` says which cycle was actually run. A Mumbai cycle takes about 8 s on the development laptop, over the 5 s of CLAUDE.md 11.1, which is Sky's budget to meet and is visible in the `stage_ms` the response carries.
- Date: 2026-09-09

## ADR-0017 A run's honesty labels state the reason the stage reported, never a reason inferred from its output

- Context: `sky_notes()` built the Z-R label by inferring the fallback reason from `ZRParams.source`, and wrote "Marshall-Palmer: 20 usable gauge-radar pairs, fewer than the 8 an adaptive fit needs". Twenty is not fewer than eight. `varuna_sky.zr` abandons the fit for three different reasons and only one is the pair count; on the real Mumbai bundle the fit was abandoned because the gauges spanned a factor of 1.6 in rain rate, too little to fit an exponent. The sentence was false, and it was printed on the console under the run stamp - exactly where a MoES scientist would read it.
- Decision: `ZRParams` carries an optional `reason`, set by whichever fallback branch fired, and the label prints that string. A note may only restate what a stage reported about itself; it may never re-derive that reason from the stage's output.
- Alternatives: word the caption vaguely enough to be true of every branch (true but uninformative, and it hides which of three real conditions fired); log the reason and leave the console silent (rule 6 wants the label where the user is, not in a log file); pass the reason as a side channel to the pipeline (a second source of truth to drift).
- Consequence: the label now reads "Z-R is Marshall-Palmer (a=200, b=1.6): 20 gauges span only a factor of 1.6 in rain rate, too little to fit an exponent." The general rule matters more than the instance: every remaining note in `sky_notes` was re-checked against this test, and the same rule binds the Twin, Pulse and Flash labels when they are written. Found by running the real endpoint against the real bundle and reading the output, which is the only way this class of bug surfaces - it type-checks, it lints, and every test passed.
- Date: 2026-09-09

## ADR-0018 The API image installs a C toolchain, uses it, and purges it in one layer

- Context: every Railway deploy of the API image failed at the dependency layer with `error: [Errno 2] No such file or directory: 'gcc'`. pysteps publishes no Linux wheel, so uv builds it from its sdist, and its Cython extensions (`_proesmans`, `_vet`) compile with `gcc ... -fopenmp`. The Dockerfile installed only `ca-certificates curl git libexpat1`, so the image could never have built - and nothing local would show it, because a working checkout resolves pysteps from the developer's own cache. Railway's own diagnosis suggested `RAILPACK_BUILD_APT_PACKAGES=build-essential`, which fixes its auto-builder rather than the Dockerfile the repo actually ships (ADR-0014, `railway.json`).
- Decision: `apt-get install libgomp1`, then `build-essential`, then `uv sync`, then `apt-get purge --auto-remove build-essential` - all in one `RUN`. `libgomp1` goes in on its own line first so apt marks it manually installed: the compiled extension links OpenMP at runtime, and `--auto-remove` would otherwise remove it along with the compiler that pulled it in.
- Alternatives: leave `build-essential` in the image (~250 MB of compiler shipped to production for no runtime purpose); purge it in a later layer (does nothing - layers are additive, so the bytes stay); a multi-stage builder (correct in principle, but this Dockerfile is single-stage and its entrypoint, volume and healthcheck logic would all need re-threading for a saving the one-layer purge already gets); set Railway's Railpack variable (fixes one platform's auto-builder and leaves `docker build` broken everywhere else, including `make pack`).
- Consequence: the image builds. The compiler is present only inside the layer that needs it, so the shipped image is unchanged in size. The second `uv sync` on line 48 installs the workspace packages, which are pure Python and need no toolchain. If another dependency later ships without a wheel, this is the line that has to know about it.
- Date: 2026-09-09

## ADR-0019 A pit no larger than one grid cell is a DEM artefact, and the threshold says so inclusively

- Context: `breach_spurious_pits` skipped any depression with `area >= min_area_m2`, and CLAUDE.md 10.1 step 4 sets that threshold at 900 m2. A single cell of the 30 m Mumbai grid is exactly 900 m2, so `>=` was true for every depression the DEM could contain: the run's own stats read `pits_before: 4004, pits_spurious: 0, pits_breached: 0`, and 4,004 of them were reported as "protected" - a number plausible enough that nobody looked. 2,418 single-cell pits survived conditioning, 47 % of Mumbai's depressions, median 0.39 m deep. In a 3-hour run each one filled past 60 cm, so the console drew its deepest water on hillsides at 15 to 21 m elevation while Hindmata, King's Circle and Sion Circle - sitting in real 3 to 5 m depressions, with drain inlets that worked - peaked below 8 cm. The hotspot rail is what surfaced it: 86 segments over 30 cm and not one chronic spot among them.
- Decision: breach when `area <= min_area_m2`. The threshold means "a pit no bigger than one cell", which is a statement about the resolution limit rather than about hydrology. `protect` - underpasses, subways, the chronic-spot register - still overrides area, which is what keeps the one-cell sinks that are real, Andheri and Milan subways among them. The kept-because-large and kept-because-protected counts are now separate stats, since conflating them is what hid this.
- Alternatives: set the threshold to 901 m2 (fixes Mumbai and silently breaks any city on a grid finer than 30 m, where one cell is far below either number); breach every pit under two cells (a threshold in cells, not area, so it stops meaning the same thing between the 30 m grid and the 5 m nests of P4.8); leave it and lower the depth ramp's floor (would hide the symptom and keep publishing 60 cm of standing water on a hilltop, which is exactly what rule 6 exists to prevent); treat unbreached pits as sinks with an outflow (invents drainage the DEM does not evidence).
- Consequence: the city must be rebuilt and every baked run re-baked, since the conditioned DEM feeds the drain graph, the surface units and the depressions-versus-register validation. Two tests at the real 30 m resolution pin the boundary in both directions - a bare one-cell pit is breached, a registered one is kept - because the existing pit tests were written on a 10 m fixture where one cell is 100 m2 and the off-by-one cannot bite. The general lesson is in the stats: a counter that merges two reasons cannot report that one of them never fires.
- Date: 2026-09-10

## ADR-0020 The hotspot rail ranks by peak depth, and says so on screen

- Context: CLAUDE.md 11.8 fixes the hotspot score as `P(impassable at peak) x exposure_weight`. Phase 4 gives one deterministic Twin run, so `P` is 0 or 1 and that product collapses to a two-tier sort with the exposure weight breaking ties inside each tier. An operator scanning 28 chronic spots gets no ordering from that.
- Decision: rank by peak depth over the run's horizon, and print "ranked by peak depth" in the rail's header. `expected_impact` is still computed and carried per hotspot, so nothing downstream changes shape when Flash-lite makes `P` continuous in Phase 7 and the spec's product becomes the right key.
- Alternatives: use the spec's score anyway (a ranking that is really two buckets, presented as a ranking); rank by minutes above the car threshold (zero for most spots on a moderate cycle, so most of the rail would tie at 0); rank by exposure weight (a property of the city, not of this forecast - the same order every cycle, which is not a forecast product).
- Consequence: the rail is useful now and does not lie about what ordered it. Depth at a hotspot is sampled as the 90th percentile over a 45 m neighbourhood, the same rule and reasoning as the road segments in `varuna_products.depth`, so a junction's answer does not depend on which side of a cell boundary the register's marker fell.
- Date: 2026-09-10

## ADR-0021 The Twin runs on the ensemble mean, because a pixelwise quantile is not a rainfall field

- Context: `twin_cycle` fed the Twin Sky's **p50** field, with a docstring arguing that the median is the statistic the console already draws. Measured on the 2 July storm, that field delivers 13 mm of rain over three hours where the truth field delivers about 100 mm at the chronic spots, and the Twin gave Hindmata 3.7 cm. The cause is not a bug in Sky: at a given pixel and lead time the 20 STEPS members disagree about *where* the convective cell is, so most of them are dry there and the pixelwise median collapses even though every member is carrying a downpour a kilometre away. CLAUDE.md 11.11 specifies the mean; the median was my own departure from it.
- Decision: Sky publishes `mean` beside `p10/p50/p90` and the cycle runs the Twin on it. The mean is the only reduction available here that conserves volume - the expectation of a sum is the sum of the expectations - so the city receives the water the ensemble actually forecasts.
- Alternatives: run the Twin on one member (one draw's noise becomes the depth map, and the member choice would change the answer); run it on p90 (a forecast of the wet tail presented as the forecast); keep the median and raise the depth ramp (hides the loss rather than fixing it, and the water balance stays wrong); wait for Flash-lite to consume the full ensemble in Phase 7 (correct eventually, and leaves Phase 4-6 producing a city that barely gets wet).
- Consequence: on the 08:40 cycle Hindmata goes 3.7 -> 10.0 cm, wet segments 503 -> 6,474 and surcharging manholes 4,101 -> 9,524. The mean smooths the convective peak, which is a real cost and is exactly why the ensemble goes to Flash whole in Phase 7; the run's notes already say the spread was discarded. Two tests state the arithmetic: twenty members carrying one cell in twenty different places have one unambiguous volume, the mean reproduces it and the median returns zero.
- Date: 2026-09-10

## ADR-0022 The mass balance counts what the drains discharge at their outfalls

- Context: the Twin's combined audit summed rain in and the tide in and out across the 2D surface, and compared that to stored volume. It never counted `boundary_m3` - the volume leaving the 1D network through its free and tidal outfalls - even though `drain1d.simulate` had been returning it all along. Water that fell on a street, was captured by an inlet and left through a pipe simply vanished from the ledger. The error was small while the Twin ran on a rain field that barely wetted the city and grew with the water: 1.5e-02 on the 08:40 cycle after ADR-0021, fifteen times the CLAUDE.md 11.3 budget.
- Decision: accumulate `drain_run.boundary_m3` and put it on both sides of the ledger - positive is discharge to sea, negative is the sea pushing back up a tide-locked trunk, which is an inflow.
- Alternatives: widen the budget (the budget is an acceptance criterion, not a preference); measure the drains separately from the surface (two balances that each close say nothing about the coupling between them, which is where the interesting errors are); treat outfall discharge as storage (it is not in the model any more, and the number would drift with every cycle).
- Consequence: 1.5e-02 becomes 6.07e-04, inside the 0.1 % budget, and 131,162 m3 that had been unaccounted on that cycle is now in the audit. The test that should have caught this had a 5 % tolerance - wide enough to pass a run that had lost 1.5 % of its water - and now asserts the spec's 0.1 %; a second test asserts the ledger closes term by term, so any flux a future stage forgets shows up whatever its sign.
- Date: 2026-09-10

## ADR-0023 Alerts and pump dispatch are scoped to streets, not only to the chronic register

- Context: both products were scoped to the 28 sourced hotspots. On these cycles the register peaks at 13 cm while 226 ordinary segments go over 45 cm, so the alert queue was empty and the pump board assigned nothing - an all-clear over a flooding city. Two things were missing: CLAUDE.md 11.10 scopes the state machine "per segment/ward", and `segments.parquet` carried no street name, so an alert about a segment could only have said "S152371623-000".
- Decision: the city pipeline keeps OSM's `name` on every segment (10,096 of 21,296 are named), products collapse per-segment series onto street names taking the worst segment step by step, and both the alert queue and the pump board take streets as candidates alongside the register. The register still leads the queue inside each level, because those are the sourced places a judge recognises. Unnamed ways are dropped rather than given a placeholder.
- Alternatives: alert per ward (the ward column is null for all 21,296 segments - the OSM admin boundary layer was never joined); alert per segment id (an alert a ward officer cannot act on); lower the thresholds until the register trips (moving the goalposts so the demo looks busy, which is rule 6 in reverse); leave the queue empty and explain it (honest, and it discards a real product the run already supports).
- Consequence: the 08:40 cycle raises 49 severe and 11 moderate alerts on named roads, and assigns all twelve pumps for about 925 minutes above 45 cm avoided. The pump benefit is a bathtub - a pump lowers the ponded depth over its 45 m neighbourhood at its rated capacity, ignoring the inflow still arriving and the drain already pulling water out - so it is labelled `reduced_model` in the API and "Bathtub estimate, not a physics run" on the board, and the emulator replaces it behind the same interface in Phase 7.
- Date: 2026-09-10

## ADR-0024 The demo runs ship in the repo, and the production console's API base ships with it

- Context: two independent reasons the deployed console was blank. Railway had no runs - `data/runs` is gitignored and a cycle takes about three minutes of CPU, so a fresh container had nothing to serve - and the Vercel build had no `NEXT_PUBLIC_API_URL`, so `apiUrl()` fell back to its development default and every request from the deployed page went to `http://localhost:8000` on the *visitor's* machine. The second one had survived because it looks like it works on a developer's laptop, where a local API is listening.
- Decision: seven baked cycles are committed under `demo/runs` (10.9 MB - everything the console reads, without the 19 MB-per-cycle `segment_forecast.parquet` that is the product of record) and `varuna_api.seed` copies them onto the volume the first time the API starts against an empty run directory. `apps/command/.env.production` carries the API base and is committed, with the app's own `.gitignore` opting it out of `.env*`.
- Alternatives: bake on boot (a quarter of an hour of 404s, on a platform that health-checks); a Railway volume seeded by hand (works once, and is lost the next time the volume is recreated); set the API base only as a Vercel project variable (invisible in the repo, and it is the setting that was already missing); hardcode the URL in `client.ts` (the same value, in the place a reader is least likely to look for configuration).
- Consequence: a fresh deployment of either half serves a working console, and `make pack`'s offline requirement (CLAUDE.md 17) is met by the repo itself. A Vercel project variable of the same name still overrides the committed default, which is how a preview points at a different API. The value is public - it is the URL the browser calls - so committing it leaks nothing.
- Date: 2026-09-10

## ADR-0025 Flash-lite is fitted per road segment, as a perturbation, and its skill is published as measured

- Context: CLAUDE.md 11.7 specifies a two-reservoir Nash cascade per **surface unit**, coupled through `drain1d`, fitted to at least 200 Twin runs, running the AOI in 300 ms. Two things about this city made the specified form unworkable as written. First, every product downstream - the map, the rail, alerts, routing - is keyed on road segments, so a unit-keyed emulator would need a unit-to-segment reduction on every call and would be fitted against a target it never predicts. Second, and much more seriously: a local-rain cascade cannot produce what the Twin produces where it matters. Segment `S215609077-002` receives 10 mm of rain and ponds 60 cm, because that water is the tide and the runoff of a catchment upstream. Fitting the whole depth field to local rain gave a peak correlation of 0.98 and a third of the amplitude - the right map at the wrong scale, and a CSI at 30 cm of 0.02.
- Decision: fit per road segment, and fit the **deviation from a base state** rather than the depth. The base state is the depth every fitting storm produced regardless of intensity, taken as the element-wise minimum over the fitting runs and measured from those runs only. The cascade predicts the deviation and `simulate` adds the base back. `/v1/whatif` then levels on the Twin's own forecast for the run and uses the emulator only for the difference the scenario makes, which is a delta-correction - the standard way to use a cheap surrogate beside an expensive model, and it keeps the number on screen the physics'.
- Alternatives: fit the full depth field (measured: CSI 0.02, and every what-if would inherit that); drive from AOI-mean rain (measured: no better - one gain per segment cannot carry every storm's spatial pattern); widen the amplitude search (measured: 0.084 to 0.085, so the limit is structural and not the optimiser); weight the objective toward wet steps and align the fitted model with the one that runs (both done, and both were real bugs worth fixing - the fit had been scoring a closed form that floors a cumulative sum while `simulate` floors each step); fit 200 runs as specified (each is three minutes of CPU; eight is the budget that existed, and the honest response is to publish the skill rather than the run count).
- Consequence: what-if runs in 62 ms and attribution over 15 candidates in 126 ms, both far inside CLAUDE.md 14's budgets. The measured skill is RMSE 5.7 cm and **CSI 0.085 at 30 cm on held-out storms**, published to `docs/verification/flash_lite.json` and printed on the what-if panel beside every answer. That number is low and it is the point: the emulator supports a *ranking* (peak correlation 0.98) and does not support a depth, so attribution uses it and the depth on screen comes from the Twin. Two consequences are enforced in code rather than documented: a tide offset is refused, because the base state belongs to one tide series; and the drainage lever is honestly weak - it acts only on the small rain-driven deviation, so cleaning a pipe moves a deep coastal segment by tenths of a centimetre. P7.6 (50-member ensemble products) and P7.7 (attribution in the hotspot drawer) are **not** claimed on this model.
- Date: 2026-09-10

## ADR-0026 Pulse's localisation is enforced, not merely weighted

- Context: the EnKF tapers its gain by hydraulic hop distance so an observation cannot move a pipe more than three hops away. With the taper applied to the gain alone, a run over Mumbai's 49,770 edges reported *every* edge updated, and the test that asserted the radius failed. The taper was working: `theta` for a distant edge was untouched. But the posterior `beta` reported for it was the mean of a fresh 50-member draw through the sigmoid, and that differs from the prior by sampling noise - about two blockage points, which is the same size as the signal the filter is trying to find.
- Decision: an edge no observation reaches keeps the prior it came in with, exactly. `reachable = (taper > 0).any(axis=0)` selects; everything else is passed straight through, mean and spread alike.
- Alternatives: raise the ensemble size until the noise is small (fifty members is CLAUDE.md 11.6's number, and the noise falls only as 1/sqrt(N) - it would take thousands); report the prior ensemble mean rather than the input prior as the baseline (makes the diagnostic consistent and leaves the noise in the product); accept it as small (it is the same magnitude as the updates the filter makes, and it accumulates over a bake because the posterior is carried forward).
- Consequence: on the 08:40 cycle 275 edges of 49,770 are updated rather than all of them, and the drain X-ray's "before / after" toggle shows a change where observations landed instead of a wash across the city. Across the seven baked cycles the worst pipe climbs from 0.394 to 0.683 as observations accumulate, which is the carry-forward doing what CLAUDE.md 11.6 asks of it.
- Date: 2026-09-10

## ADR-0027 The map's camera is controlled, and it frames what is drawn

- Context: the console opened with Mumbai in a corner of a half-empty panel and `/drains` rendered the pipe network as a thumbnail in the middle of nothing. `CityMap` passed its computed fit to deck.gl as `initialViewState`, which deck reads once when the view is created - a frame *before* the first `ResizeObserver` callback has measured the container. The fit therefore never applied. That code had been written to dodge an ESLint `react-hooks/set-state-in-effect` error, and dodging the lint rule is what broke the feature.
- Decision: the camera is controlled. Two things are state - the measured container, set from the `ResizeObserver` callback, and the camera once somebody moves it - and the fit is **derived** from them during render, so a resize or a data load re-frames on its own with no effect and no stale copy. It frames the drawn geometry rather than the city's configured AOI: a route on `/route`, the drain graph on `/drains`, the street network on the console. `interactionState` separates the operator's drags and zooms from deck's own resize adjustments, because treating both as "the operator took the camera" left `/route` framed on the whole city after a resize instead of on the trip it had just drawn. The one remaining `set-state-in-effect` disable is on the fly-to, with the reason written beside it: `focus` is an imperative command from the rail, not derived state, and deriving it would pin the camera so the operator could never pan away from a selected hotspot.
- Alternatives: keep it uncontrolled and call `deck.setProps` imperatively (an escape hatch around React for something React does well); fit once at mount (what was intended, and it cannot work with an async first measurement); set state during render with a ref guard (React's documented pattern for adjusting state on a prop change, but `react-hooks/refs` forbids the ref write and the guard).
- Consequence: both maps fill the space they are given at 1366 x 768 and at 1440 x 900. `/drains` needed a second fix in the same pass: it was a grid item with `min-h-[560px]` beside a 25-row table, and a grid row stretches to its tallest item, so the canvas had grown to 7,196 px and the viewport was showing the empty top of a very tall picture. It is a two-pane screen with the viewport's height now, and the panels beside the map scroll on their own.
- Date: 2026-09-10

## ADR-0028 A route is scored at the arrival time, and the served shape supersedes the Phase 0 draft

- Context: CLAUDE.md 11.9 specifies `c_e(tau, v) = t_e * phi(h_e(tau))` - the cost of an edge read at the time the vehicle reaches it. A router that reads the depth at the departure time answers a different question from the one an ambulance leaving KEM at 08:40 actually has, and on the 2 July storm the two answers differ.
- Decision: time-dependent Dijkstra keyed on arrival time, over a road graph held in flat CSR arrays rather than a `networkx` object (a route has a 300 ms budget and a reachability sweep visits 34,708 directed edges). FIFO is asserted in the test suite rather than argued in a comment, because it is what makes Dijkstra on arrival time exact. The response carries **both** routes - naive and VARUNA - because a dispatcher shown only the safe route has no way to judge whether the detour was worth it.
- Alternatives: cost at the departure time (simpler, and wrong for exactly the trip the demo makes); `networkx` with a time-dependent weight function (Python object traversal spends its time on attribute lookups; measured, the flat arrays route in 85 ms); return only the VARUNA route (unfalsifiable - of course the system says its own route is fine).
- Consequence: KEM to Sion plans in 85 ms with two alternates. Two defects were found getting the graph right, both silent: `segments.parquet` is EPSG:32643, so a first version compared UTM metres against degrees and snapped every request to the same junction; and `class` is a Python keyword, so `itertuples` never carried it and every road took the default speed. **The served response supersedes `varuna_schemas.models.route.RouteResponse`**, the Phase 0 draft the stub declared: that model carries `confidence` and `confidence_note` fields nothing computes, and inventing them to satisfy a draft would be rule 6 in reverse. The draft stays in the schema package as the pilot contract; the OpenAPI test asserts on the models actually served.
- Date: 2026-09-10

## ADR-0029 Verification sweeps the threshold instead of picking one

- Context: CLAUDE.md 11.12 asks for "CSI/POD/FAR for 'above 30 cm within the window' at chronic spots and pins". The 29 curated pins for 2 July 2019 record *waterlogging* - "traffic diverted", "water in the subway" - and not one of them states a depth. Scoring them against a single 30 cm line treats a civic log as though it had said "over thirty centimetres", which it did not, and it produces one number that hides the choice.
- Decision: score at 5, 15 and 30 cm and publish all three, with 15 cm as the headline because it is about the least water a Mumbai civic log would call waterlogging. False alarms are counted only within 250 m of some pin, because nobody logged most of the city that morning and counting every unpinned wet street would score the record-keeping rather than the forecast; the figure is labelled a lower bound. The median lead time excludes hits made *after* the pin was logged - they are real hits, since a pin whose stated uncertainty is twelve hours cannot rule them out, but averaging them into a number the product sells as "three hours early" would flatter it, so they are reported separately.
- Alternatives: report 30 cm alone (CSI 0.09, and the reader cannot tell whether the pattern or the level is wrong); report 5 cm alone (POD 1.00, which is flattering and equally uninformative); assign depths to the pins from their wording (fabricated ground truth, rule 7).
- Consequence: at 15 cm, CSI 0.22, POD 0.41, FAR 0.68, median lead 31 minutes over six pins. The spread across the three rows is the finding and is printed on the screen: every pin is found at 5 cm and only two at 30 cm, so the pattern is right and the level is low - a different problem from missing the streets. Depth MAE, the Brier score and the reliability diagram are returned as `unavailable` with their reasons (no pin states a depth; the baked runs have one member) and the screen renders that panel rather than three blanks.
- Date: 2026-09-10

## ADR-0030 A design storm is not nowcast

- Context: the onboarding wizard's first forecast for Chennai was empty. The `CHN-IDF-25yr` bundle specifies 150 mm over three hours - the drainage-norm design intensity, 50 mm/h, held over the forecast horizon and shaped as a Chicago hyetograph - and the AOI received **0.1 mm**. Issuing the cycle at the storm's peak frame instead made it worse in the other direction: persistence held the 447 mm/h Chicago spike for three hours and delivered **609 mm**, a metre of water on the median street.
- Decision: a design-storm bundle forces the Twin from its own truth field. Sky still runs - for its grid, its stage timing and its honesty labels - but the *forcing* is the hyetograph the bundle states. A reconstructed event still goes through the nowcast, because there the nowcast is the thing being demonstrated. The run's notes say which path it took.
- Alternatives: issue from the peak frame (609 mm from a 150 mm storm); issue from the median frame (0.1 mm - the ensemble mean has collapsed either way); scale the nowcast to the known total (inventing a correction factor to hide a structural mismatch, rule 6).
- Consequence: Chennai's first forecast delivers 142.6 mm of the specified 150 mm, mass balance 6.0e-04, 15,468 of 18,622 segments wet with a median street peak of 26 cm and p90 of 78 cm. **The diagnosis is the point**: pySTEPS STEPS advects and perturbs *spatial structure*, and a design storm is spatially uniform by construction, so its twenty members decorrelate into noise and their pixelwise mean collapses - the members carried the 11.5 mm/h the radar showed while the ensemble mean peaked at 3.3. This is the same failure mode as the median-versus-mean defect fixed earlier, one level deeper: the mean is the right reduction and it still cannot rescue an ensemble whose spread is entirely artificial. `MUM-IDF-25yr` was affected identically.
- Date: 2026-09-10

## ADR-0031 A city nobody has researched still builds

- Context: `make city CITY=chennai` stopped dead at step four. The hotspot register and the curated infrastructure file are hand-written from civic logs and news with a `source_url` on every entry (CLAUDE.md 10.1 steps 8 and 9, rule 7), they exist only for Mumbai, and their absence raised. Two thirds of the pipeline never ran, so the wizard whose entire claim is "a new city in minutes" could not produce one.
- Decision: a missing register writes an empty one that says why, and a missing infrastructure file falls back to the OSM facilities alone. Nothing is invented to fill either. The city's own depressions are the hotspot *candidates* for a new city - which is what CLAUDE.md 10.1 step 4 already calls them - and OSM still supplies the hospitals, fire stations and stations that reachability and exposure actually read.
- Alternatives: keep raising (the wizard cannot work); generate a register from the depressions and label it curated (fabricated ground truth); ship a hand-written Chennai register (real research, and not something to invent under time pressure).
- Consequence: Chennai builds end to end from cache in **36 s** - 18,622 segments, 40,729 inferred pipe edges, 1,854 depressions, 6,147 surface units, 444 assets all carrying a `source_url` - against the wizard's 5-minute budget. Two further gaps in the same class were fixed alongside: `gauge_window` raised on a bundle with no gauges, where Sky's Marshall-Palmer fallback is exactly the designed behaviour, and the Twin refused a coastal domain with no tide series, where mean sea level is the neutral assumption and is now noted as an assumption on the run. `--cache-only` also fetches OSM and land cover now, rather than only verifying DEM tiles, so the pre-cache is actually a pre-cache.
- Date: 2026-09-10

## ADR-0032 An Overpass reset is not a fact about the city

- Context: Chennai's `waterways` layer came back with 0 features and a logged warning. The cause was a connection reset from Overpass under load; the effect was a drain graph built with no river outfalls, recorded as though the AOI had no canals. An empty layer and a failed fetch are very different things and only one is a reason to distrust everything downstream.
- Decision: three attempts per layer with a widening pause, and a `fetch_errors` map on the layer set so a failure is reported rather than swallowed.
- Alternatives: fail the build on any layer error (one flake on a shared public service kills a ten-minute download); leave it as it was (a silent wrong answer, which is the worst of the three).
- Consequence: the retry recovered Chennai's waterways from 0 to 54 on the first attempt.
- Date: 2026-09-10

## ADR-0033 A second city means every "latest run" needs a city

- Context: onboarding Chennai put `CHN-` runs into the same `data/runs/` as Mumbai's. Run ids sort chronologically and `CHN-2026...` sorts above `MUM-2019...`, so every endpoint that resolves "the current run" by taking the newest directory began answering a Mumbai console with Chennai water - the depth raster, the segment forecast, the drain health, the route, and the run stamp in the top bar. Nothing errored and nothing looked wrong; the console simply showed a different city's flood under Mumbai's street names.
- Decision: one shared helper, `varuna_api.runs_util.latest_run_for(city, requires=...)`, filtering on the run-id prefix, used by the depth products, what-if and the route service; `/v1/runs` defaults to the configured city, with `city=all` as the explicit way to ask for the whole registry.
- Alternatives: fix the four call sites separately (the fifth would have been written city-blind next week); scope by directory per city (a bigger migration, and run ids already carry the city); read the city out of each `run.json` (an extra file read per candidate, for information the name already has).
- Consequence: a silent wrong answer became impossible to reproduce by adding a city. The cost is that a caller who genuinely wants "any city" has to say so, which is the right way round.
- Date: 2026-09-11

## ADR-0034 The map labels itself from two sources

- Context: the satellite basemap gave the console texture - a judge could see a real city - but nothing on it was named, so "which junction is that?" had no answer on screen. CLAUDE.md 6.7 asks for a quiet label layer at z >= 12 and z >= 15.
- Decision: two layers. Esri's reference tiles carry localities and arterials; VARUNA's own `TextLayer` carries the things this product is *about* - street names from the segment layer, hospitals and fire stations from the asset register, chronic junctions from the hotspot register. Both are drawn **over** the depth ramp, because a street name the water paints over is a name nobody can read. Everything is zoom-gated, and the 260-label cap applies to what is *in view* rather than to the whole city, so zooming in reveals more of the network.
- Alternatives: basemap labels only (they do not know Hindmata is a chronic spot); our own labels only (no localities, and a city reads by its neighbourhoods); no cap (21,296 street names is a grey smear).
- Consequence: the label set is derived from a **quantised** camera - a tenth of a zoom level, ~100 m of position. Deriving it from the exact view state recomputed it on every frame of a pan, which rebuilt the layer array sixty times a second and left the tile layers being reconciled instead of drawn; the imagery took twenty-five seconds to appear. One `IconLayer` sprite carries a raw `#fff`, marked with the linter's own escape: `mask: true` makes deck read only the alpha and tint with `getColor`, so the fill is not a colour and a token there would mislead.

## ADR-0035 The drain solver and the coupling are compiled; the 8 s budget still needs a different algorithm

- Context: P4.6 asks for a three-hour AOI run in 8 s. It was ~115 s, and the cost was not the 2D surface solver (already Numba, 15 s) but the two NumPy stages around it: `drain1d.step` at ~106 s, called 10,800 times at a 1 s inner step over 49,770 edges, and `coupling.compute_exchange` at ~25 s, called 2,160 times over 50,110 nodes with two `np.add.at` scatters. Both were correct and slow for the same reason: every `np.where`, `np.minimum` and `bincount` allocates, so one step walked 50k-element buffers about thirty times to do a few hundred megaflops.
- Decision: two Numba kernels - `drain_kernel.step_kernel` (three passes: edges, nodes, edges) and `coupling_kernel.exchange_kernel` (one pass over nodes, with the cell scatter as an ordinary accumulating loop). The NumPy versions stay as the specification and the readable statement of CLAUDE.md 11.4, 11.5 and Appendix A; `test_drain_kernel.py` asserts the two agree on flap gates, tide-locked outfalls, pressurised nodes and a supply-limited node. Single-threaded, because the node accumulations are scatter-adds and a parallel reduction would vary the float addition order with the thread count (rule 8).
- Alternatives: `prange` over edges (races on the scatter); a larger inner step (changes the physics CLAUDE.md 11.4 fixes); leaving it (the live-cycle budget is unreachable, and every `make bake` costs two hours).
- Consequence: **115 s to 74 s**, with identical physics - peak 251.7 cm and mass balance 6.1e-04 either way. Coupling 25 s to 4.6 s; drain 106 s to 29 s. **The 8 s budget is still missed**, and by a route micro-optimisation cannot close: the kernel is doing 537 million edge-updates of genuine arithmetic, and the remaining factor of seven needs parallelism the scatter currently forbids. The way in is edge colouring - partition the edges so no two in a group share a node, then `prange` each group - which is a real piece of work rather than a tuning pass. Two silent defects were fixed on the way: `ExchangeResult` was returning shared scratch buffers, so two results aliased each other and the second overwrote the first (a kappa test caught it; the run loop never would have); and both kernel caches were keyed on `id()`, which CPython reuses after a collection, so a freed network's arrays were handed to a longer one and Numba - which does not bounds-check - scattered past the end of a buffer into an access violation in an unrelated test.
- Date: 2026-09-11
- **Superseded in part 2026-09-24 by ADR-0074.** The colouring this named is built and 3.5x faster on the drain step; the "remaining factor of seven" was not there to win, because the drain is 42 % of the run.

## ADR-0036 `--text-3` was below the contrast floor the spec sets for it

- Context: CLAUDE.md 6.10 requires 4.5:1 for text and CLAUDE.md 6.2 fixes `--text-3` at `#6E7E9E`. Measured, those two sections contradict each other: `#6E7E9E` gives **4.25:1 on `--deep`** and **3.84:1 on `--well`**, and only scrapes 4.64:1 on `--ink`. It is the colour every secondary line on every screen is written in - the hints under a layer toggle, the run stamp, the "peak 4 cm at 12:10" under each hotspot - so the failure was everywhere at once. axe found it on twelve of thirteen screens.
- Decision: `--text-3` becomes `#7E8DA9`, which passes on all three surfaces (4.68:1 at its worst, on `--well`) and is the smallest change that does. The token is the source of truth, so `tokens.json` changes and `globals.css`, the Tailwind theme and `ramps.py` follow from the build; CLAUDE.md 6.2's table is corrected to match, because a spec that states a failing value is worse than no value.
- Alternatives: keep the hex and relax 6.10 (the floor is the accessible-design commitment, and the hex is an aesthetic preference - the floor wins); lighten only where it sits on `--well` (one token with three values by context is not a token).
- Consequence: the muted text is a shade lighter everywhere and still reads as muted. Three other classes of violation were fixed in the same pass, all of them real: links inside text blocks were distinguished by colour alone (`hover:underline` is invisible until you are already pointing at it); Base UI's `Slider` renders one hidden `<input type="range">` per thumb and an `aria-label` on the root never reaches it, so **every slider in the app shipped unlabelled**, the console's scrub bar included; and five scrollable regions held nothing focusable, so a keyboard user could see the first screenful of the CAP document, the phone mock's messages, the public map's street list or a wide table and never reach the rest. 13 screens, 0 violations.
- Date: 2026-09-11

## ADR-0037 The landing page's Lighthouse gate is its metrics, not its composite score

- Context: CLAUDE.md 14 asks for Lighthouse performance >= 0.90 on the landing page. Measured against a production build it is **0.72**, and the breakdown says why: FCP 0.4 s (score 1), **CLS 0** (score 1), **LCP 1.6 s** (score 0.77, and inside the spec's own 2.5 s budget), TBT 310 ms (score 0.57), and **Speed Index 4.6 s — score 0.05**. Speed Index measures how quickly the above-fold image *stops changing*, and CLAUDE.md 7.1 requires the hero to be a map auto-scrubbing three hours in an eight-second loop (motion M1). The two requirements are in direct conflict: a hero that scores well on Speed Index is a static hero.
- Decision: assert the metrics the spec actually names - LCP < 2.5 s, CLS < 0.1, accessibility >= 0.95 - as errors, and keep the composite performance score as a *warning* with the reason recorded here. Best practices and SEO are asserted too, since both measure 1.00 and nothing about the design fights them.
- Alternatives: make the hero static (it is the landing page's one argument, and CLAUDE.md 6.1 spends the boldness there deliberately); leave the composite as an error and let CI fail forever on a deliberate design choice (a red build nobody can act on trains people to ignore red builds); drop the audit (then LCP and CLS regress unnoticed).
- Consequence: LCP 1.6 s, CLS 0, accessibility **1.00**, best practices 1.00, SEO 1.00, performance 0.72. The console map is `next/dynamic` now rather than in the landing page's first bundle - the hero's opening seconds are an SVG globe that needs none of deck.gl - which took LCP from 3.5 s to 1.6 s. 336 KiB of unused JavaScript remains and is warned on; the next real win is there rather than in the hero.
- **Note on measurement**: the first run of this scored 0.41 with a total blocking time of 11 seconds and "unminified JavaScript" among its opportunities. That was Lighthouse finding a dev server already listening on port 3000 and auditing Turbopack's development output. Anyone re-running this must stop `make dev` first, or they will be optimising a build that never ships.
- Date: 2026-09-11

## ADR-0038 The onboarding wizard fetches its own open data, and reports the step that broke

- Context: city-in-a-box worked locally and failed on every run of the deployed site. `GET /v1/onboard/city/chennai` came back `failed` at step `build_graph`, 83 % - and the log underneath said the real cause: the `cache` step, the *first* of fourteen, could not find the Copernicus and WorldCover tiles. Two independent defects. **(1)** `run_city` does not stop at a failed step; it records the failure and runs the remaining thirteen, each failing for want of an output the first never wrote, and the job's log tap advanced `step` and `progress` on every one. The wizard therefore named the last step it saw rather than the one that broke, so a judge would watch the build die at "Build graph" at 83 % when the open data was missing at step one. **(2)** The tiles were absent by design: `varuna_city.cache` never downloads ("Phase 1 never downloads" - it verifies against `MANIFEST.json` and raises), and the Railway entrypoint's `drop_download_cache` deletes the 339 MB cache after the first build because the 500 MB volume cannot hold both it and the 116 MB city it produces. The wizard demanded a cache the deployment had removed on purpose. `from_cache_only` was no help: the request carried it, `OnboardState` stored it, the log line printed it, and nothing read it - and had it been passed to `run_city(cache_only=...)` the build would have been *worse*, because that parameter means "run only the fetch steps", the opposite thing.
- Decision: the job warms its own cache before building. `_warm_cache` verifies the tiles; if they are present it returns having touched no network, which is the stage path. If they are absent it either refuses - naming `tools/prefetch_city_cache.py` - or fetches just that city's tiles, streaming the downloader's own output into the log. `from_cache_only` now decides which, `VARUNA_OFFLINE=1` overrides it either way (CLAUDE.md 4.4), a free-space check runs before the download rather than after it fills the volume, and the tiles this job fetched are released once the build has consumed them. Separately, the log tap freezes `step` and `progress` at the first `city.step_failed`, having first mapped `step` to the failing step's wizard row.
- Alternatives: ship the tiles in the image (139 MB of redistributable-but-huge raster in a layer, and it would still be deleted by the entrypoint); stop `run_city` at the first failed step (wrong - the steps are not strictly dependent, and a failed `hotspots` step should not prevent the export, which is exactly the resumability the entrypoint relies on); grow the Railway volume (Railway ties volume size to the plan, and the fix should not need one); let the wizard fall back to OSM landuse when WorldCover is missing (CLAUDE.md 10.1 step 3 does document that fallback, and it is the right long-term answer for a city with no cached tiles at all - but it changes the imperviousness and CN rasters, so it is a physics change, not a deployment fix).
- Consequence: the Chennai wizard runs end to end in **65.4 s** locally from a warm cache, publishing `CHN-20260701T0040Z-...` with **15,468 wet segments** - the same count R8 recorded, so the build is still deterministic (rule 8). A cold cache now costs a 138.6 MB one-off download against the 188 MB free on the volume, released again afterwards. Seven new tests cover the freeze and the three cache paths. Two stale gates were found while verifying and are fixed: `onboard-screen.test.tsx` still asserted the Phase 9 placeholder ("the wizard runs from city/cache/chennai in Phase 9") and had been failing since P9.6 made the wizard real, and `verify-screen.test.tsx` still asserted the pre-P9.7 empty grid - so `pnpm test` had been red for a day. `uv run pytest` was red too, and worse: `test_drain_kernel.py` imported `tests.test_drain1d`, which resolves to the repository's own `tests/` package from the root, so the **whole suite died on a collection error before running a single test**. It only ever passed when pytest was pointed at `services/twin`.
- Date: 2026-09-11

## ADR-0039 A chronic-waterlogging point is not a building, whatever OpenStreetMap says

- Context: CLAUDE.md 10.1 step 4 burns building footprints +5 m and keeps "underpasses/subways as sinks (OSM `tunnel`/`layer<0` + the hotspot register)". `condition_dem` resolved the footprints first and the sinks afterwards, and passed the sink mask only to `breach_spurious_pits`. So the register protected pits from breaching but never protected a *cell* from being burned. Two sourced chronic points sit on an OSM building footprint - **Khar Subway** and **Parel / Bharat Mata Cinema**, both named in section 3.3 - and both were raised exactly 5 m. Worse, `buildings_mask` is also what `roughness` turns into the solver's blocked mask, and section 11.3 gives blocked cells no flux: the Twin could never put water on two of the ten hotspots the spec names. Parel is 400 m from KEM Hospital, the origin of the ambulance trip in section 3.3. Nothing failed - the build was green, all four validation targets were met, and 82.1 % of the register was still explained - because a hotspot that can never flood is indistinguishable from one that simply did not.
- Decision: resolve the points before the burn and clear them from `buildings_mask`, which fixes the elevation and the blocked flag together. Two separate claims, so two separate inputs: `sinks` (the `is_sink` subways) still override the pit-area rule, because that is a claim about topography; the new `register` argument covers all 28 points and only clears the footprint, because that is a claim about evidence. Only 5 of Mumbai's 28 points are `is_sink`, and Parel is not one of them, so narrowing to the flag would have fixed Khar and left Parel blocked - which is exactly what the first attempt did.
- Alternatives: clear the whole footprint polygon containing a register point (deletes real buildings on the evidence of one point, and at 30 m a footprint can span a block); drop the burn where a road is carved (the register points are junctions, so this would work by accident at Parel and not at a mid-block sink); flag it in `REPORT.md` and leave the physics (rule 6 - the console would keep drawing a dry street at a junction that is on the register *because* it floods); mark the two points `is_sink` by hand (makes them override the pit-area rule too, which claims a topographic sink neither source evidences, and leaves the next city's build to rediscover this).
- Consequence: 11 sink cells are cleared of a footprint, 0 register points are blocked and 0 are raised, down from 2 each. `sinks_cleared_of_building` is a recorded stat and a `REPORT.md` row, so a future regression is visible rather than silent. The city had to be rebuilt from `condition` onward - the conditioned DEM feeds the drain graph, the units and the depressions - which moved the inferred pipe to 1,716.34 km and the units to 10,466; the four validation targets are unchanged. A test at the real 30 m resolution pins it: a registered sink under a footprint is neither burned nor left in the building mask, while the footprint around it still is. **Every baked run predates this and should be re-baked** before the depth at Parel or Khar subway is quoted. The general lesson matches ADR-0019's: a mask that serves two purposes will be correct for the one its author was thinking about.
- Date: 2026-09-12

## ADR-0040 Sky's ensemble mean carries ~20 % less rain than the analysis at the first lead

- Context: CLAUDE.md 11.1 names three tests for Sky and only two were written. Writing the third - "total rain of the ensemble mean over the domain is within 15 % of persistence at lead 0" - during the Phase 3 audit showed it fails. **The first measurement was taken on the wrong field and its conclusion was too strong; this entry replaces it.** The shipped fixture is one Gaussian on a *uniform* 2 mm/h background, and pySTEPS fits AR(2) per cascade level from the lag correlations of the advection-corrected frames - over a constant background those come out of numerical noise. On the 120 px domain it measures a **negative** lag-1 correlation at the largest scale, phi_0 goes to ~0.99, the forecast is almost pure noise, and the cell flattens from 36 mm/h to 12 mm/h. **That collapse is the fixture, not Sky:** on `bundles/MUM-2019-07-02/radar/frames.zarr` the same pipeline holds the peak (70.5 -> 68.6 mm/h), p99 (35.1 -> 38.4) and the exceedance fractions (>40 mm/h: 0.65 % -> 0.82 %). Re-measured on five storms from the same seeded generator that writes the bundle, sampled mid-storm where a demo cycle sits, the **mass** bias is real and systematic: **median 21.7 % low, range 6.1 % to 27.3 %** (the bundle's own calibrated storm is the favourable end at 6.7 %). Every member loses it individually (8 members, 32.0k-33.3k against 39.3k), so it is the nowcast step and not the averaging, and wet-cell count barely moves - the field loses intensity, not area.
- Decision: keep `probmatching_method="cdf"`, ship the test on the designed field with `xfail(strict=True)` carrying the measured numbers, and record here that the obvious fix is worse. `probmatching_method="mean"` **passes** this test (+13.2 % median, inside 15 %) and must not be adopted: it inflates the 99.9th percentile by **+51.6 %**, which in a flood nowcaster is a phantom cloudburst driving phantom alerts and phantom pump dispatch. Trading a known low bias in the total for an unknown high bias in exactly the quantity that causes flooding is the wrong trade, and it would have been invisible if the only thing measured were the spec's total-rain sentence. The dBR round-trip was ruled out (-0.01 %) and `mask_method` makes almost no difference (None and `incremental` are identical; `obs` is 2 points better).
- Alternatives: widen the tolerance to 25 % (the spec sets 15 %, and moving a stated acceptance criterion to fit the measurement is what rule 6 exists to stop); switch to `mean` (above - trades a safe error for a dangerous one); leave the test unwritten (its absence is exactly why this went unseen through Phase 3's sign-off); `skip` it (says nothing and never reports); fix the nowcaster now (a change there invalidates every baked run and needs its own verification pass, not a drive-by inside an audit).
- Consequence: the ensemble is biased low in total rain at short lead - 0 to 30 minutes, where an operator trusts the forecast most - so the Twin is forced with less water than the radar saw and every depth downstream inherits it. It is a partial explanation for the low scores already on the board (`/verify` CSI 0.22, Flash-lite CSI 0.085), and `services/verify/rain_skill.py` scores against `truth/rain.zarr` rather than persistence, so it has been measuring this without naming it. Phase 3 stays at 90 % with this as the open item; the next pass on it should start from the cascade correlations and the fractional-timestep path (the cycle asks pySTEPS for 0.5, 1.0, 1.5 ... of a 10-minute step), not from the matching flag.
- Date: 2026-09-12 (supersedes the first version of this ADR, written the same day on the degenerate fixture)
- **Superseded in part 2026-09-24 by ADR-0075.** Most of the lead-0 deficit was pySTEPS' dBR blend of interpolated leads, now corrected; the whole-step deficit this recorded is still open.

## ADR-0041 Twenty-two of the API's forty-six routes declare no response model

- Context: regenerating `apps/command/openapi.json` during the P5.8 audit moved it from 41 paths to 46 - five live routes had never been in it, all five already called from `apps/command/lib/api/` - but the schema count fell from 85 to 56. The 29 that vanished are response models: `RouteResponse`, `HotspotList`, `Pump`, `Observation`, `VerificationSummary`, `DrainHealthProduct` and the rest. They disappeared because `stubs.py` declared `response_model=` on seven of its eight routes, and the real handlers written to replace those stubs return bare `dict[str, Any]`. By router: `depth` 11 routes and 0 models, `route` 4 and 0, `onboard` 3 and 0, `reports` 2 and 0, `verify` 1 and 0, `whatif` 1 and 0. The models still exist in `packages/schemas` - 13 of the 14 checked - but they no longer describe what the API returns: `/v1/nowcast/hotspots` fails `HotspotList` validation with 158 errors, `/v1/pumps` fails `Pump` with 20, `/v1/verification` fails `VerificationSummary` with 81. So the declared contract and the implementation have diverged in both directions at once.
- Decision: do not wire `response_model=` onto the existing models, and do not tick this as part of P5.8. FastAPI validates and filters responses against a declared model, so attaching a model the handler contradicts converts a working endpoint into a 500 - the console currently renders these screens. The gap is recorded here as its own piece of work: for each of the 22 routes, decide whether the model or the handler is right, then make them agree. P5.8 itself is satisfied by what it actually asks for - `pnpm typegen` producing current types, and API contract tests - and those landed with a drift guard that would have caught the five missing paths on the day they appeared.
- Alternatives: attach the models now and fix the fallout (a 500 on a demo screen is a worse failure than an untyped one, and the fallout is 22 routes wide); delete the stale models (they are the only written record of the intended shapes, and several are close); generate models from the handlers' actual returns (encodes today's accidents as the contract); leave it unrecorded (the schema count falling by 29 in a routine regeneration is exactly the kind of silent loss that needs a name).
- Consequence: for those 22 routes the console's generated types are empty, so `apps/command/lib/api/*.ts` hand-writes the shapes - two sources of truth, and no compiler check that they agree with the server. Section 12's own header, "every response carries `run_id` and `valid_ts`", is unverifiable from the document for half the surface. `services/api/tests/test_contract.py` asserts the invariants that can be checked against live responses (the error envelope, the command-naming 404, content types, the `/v1` prefix) precisely because the schema cannot carry them today.
- Date: 2026-09-12

## ADR-0042 Cleaning pipes cannot move a junction on Flash-lite, and the 4:30 beat says so

- Context: CLAUDE.md 7.2 puts "cleaning these 14 pipes: 55 -> 20 cm" in the hotspot drawer and section 15's 4:30 beat says "clean 14 pipes -> Hindmata drops". Measured on this laptop against `data/train/flash_lite.npz` and the baked runs, neither is reachable. Cleaning **all 21,296 segments** to beta = 0.05 - the whole city desilted at once, which is the largest cleaning scenario that exists - moves the deepest street by **3.466 cm** (mean 0.1416 cm, 2,032 segments over 0.5 cm) on `demo/runs/MUM-20190702T0310Z`, and by **1.519 cm** (mean 0.0334 cm, 186 segments) on the newest cycle. So a 35 cm drop is not a tuning gap; it is ten times the ceiling. Worse, the fourteen pipes cannot be the ones that matter: cleaning every pipe *except* one target leaves that target's peak exactly where it was - 71.5821 cm against a base of 71.5821 cm at the deepest street, 1.3912 against 1.3912 on a mid-range one, 7.3057 against 7.3057 at Hindmata's own deepest segment with 0 of its 24 sibling candidates scoring anything at all. The cause is not a fit: `simulate()` (`services/flash/varuna_flash/model.py:130-187`) is element-wise per segment, every term indexed by segment, with `baseline_cm` - the tide and the upstream routing, which is where most of the depth comes from - independent of beta entirely.
- Decision: attribution and the cleaning beat are **not claimed on Flash-lite**. `attribute()` already drops candidates under `ATTRIBUTION_FLOOR_CM` and refuses with `NO_ATTRIBUTION_REASON` naming the missing hydraulic operator, so the code does not lie; this entry fixes the *script*, which does. Section 15's 4:30 beat becomes "rain +30 % in under a second with the emulator's measured skill printed beside it" - the one thing the emulator does well and fast, 62 ms with its RMSE and CSI on screen beside the answer.
- Alternatives: leave the beat and let the ceiling be discovered on stage in front of MoES scientists (the worst outcome available, and a rehearsal would not catch it because 3.5 cm on a 71 cm street still *looks* like something moved); fit harder (the limit is structural, not statistical - ADR-0025 already measured that, and a per-segment cascade has nowhere to put pipe-to-street connectivity); put `drain1d` in the attribution loop now (it is the right fix and it is a phase of work - 21,296 candidates at a three-minute Twin-coupled run each, which is why P7.12 exists); show the ranking as zeros with a caveat (a row reading 0.00 cm claims the pipe was evaluated and ranked, which the number does not support).
- Consequence: the demo has no cleaning moment, and the drain X-ray's before/after - which is Pulse's *posterior* changing, not a what-if - carries the learning story instead. `docs/SIMPLIFICATIONS.md` names the ceiling with the label printed beside the "Clean top 14" switch, and `docs/QA.md` carries the figures beside the existing Flash-lite skill numbers, so the answer to "can you show cleaning?" is a number rather than a hedge. P7.7 stays open and its content is now explicit: attribution needs `drain1d` inside the loop or the GNN surrogate of P7.12, either of which restores cross-segment sensitivity. The general lesson: an emulator fast enough to answer a *why* question is not thereby able to - the question needs a coupling the speed came from removing.
- Date: 2026-09-12
- **Superseded in part 2026-09-26 by ADR-0071.** Every measurement here stands and Flash-lite still cannot attribute; the conclusion that the question is unanswerable does not, because attribution now runs on `drain1d`.

## ADR-0043 The Playwright job runs what a clean clone can show, and names what it cannot

- Context: the `e2e` job (`needs: [web]`) had been skipped since 2026-09-10 behind a red Web job, so the suite had never run in CI. The first three times it did - runs 34757943851, 34758921612 and 34760677943 - it sat in `playwright test` for 18, 35 and 13 minutes until the next push to `main` cancelled it, with no `timeout-minutes` (so GitHub's 360) and a report upload gated on `failure()`, which a cancellation is not, so none of the three left an artifact. The three logs are identical test for test, and they rule out the two obvious suspects: `uv sync --all-groups` does run, uvicorn reports startup complete within 5 s, and `next dev` is not slow - the first 21 tests took 3 m 15 s. **The time went to three `/route` tests** (demo 5:20, motion M14, "motion on") that each called `click()` on a "Find route" button that was disabled and stayed disabled: it waits on `/v1/route/facilities`, which reads `city/mumbai/map/assets.geojson`, and `city/` is gitignored. Under `test.slow()` a click may wait 360 s, and `retries: 1` waits again: twelve minutes each, thirty-six for the three. Five more failed fast on the same missing city - four zero-console-error assertions tripped by 404s from `/v1/city/mumbai/layers/{segments,assets,drains}` and `/v1/verification`, and the Phase 0 smoke on `/console` - and one was a real defect: `/route`'s right column scrolls with nothing focusable in it until the city loads (axe `scrollable-region-focusable`). Nothing needed a bundle cube or a baked rain product. Reproduced in a worktree with no city, API on :8100 and UI on :3100: the same six failures and 39 passes in 6.2 minutes with the three route tests held out.
- Decision: a test declares the data it needs with a tag - `@needs-city`, on eight tests - and `tests/e2e/requirements.ts` skips it, with "run `make city CITY=mumbai`" as the reason, when `city/mumbai/` is not on disk. CI runs the whole suite, so every untagged test must pass on a clean clone; `make e2e` on a machine with the city runs all of it. The check reads the files the API reads, never the API, so a city that is built but not served fails instead of skipping. The job is bounded three ways - Playwright's `globalTimeout` at 15 minutes so it stops and still reports, the step at 20, the job at 30 - the report uploads on every outcome, and `tests/e2e/summary.mjs` puts each test that did not run, with its reason, on the job summary page. The three route tests assert "Find route" is enabled before clicking, so a missing facilities list fails in 45 s and says so, and the `/route` scroll region is focusable, as P10.3 made five others.
- Alternatives: build the city in CI (Copernicus, Overpass and WorldCover downloads under a workflow-wide `VARUNA_OFFLINE=1`, a 339 MB cache and minutes of build on every run); commit a clipped city as a fixture (a second city to keep in step with the pipeline, whose segment ids the committed demo runs would also have to match); `--grep-invert @needs-city` in the workflow (the held-out tests vanish from the report instead of being listed beside the command that runs them); serve `next build && next start` (the dev server was not the cost, and `make e2e` would then test a different server from CI); raise the timeouts (hides the hang rather than naming it); skip when the API answers 404 (turns a serving regression into a skip - the failure this suite most needs to catch).
- Consequence: the CI job is a clean-clone smoke with a known ceiling, not the demo test. It does not exercise the route planner, the console's zero-console-errors guarantee on `/console`, `/drains` and `/verify`, or `/verify`'s scores; those stay with `make e2e` on a machine with the city, and every CI run lists them by name on its summary page. A new test that needs the city and is not tagged fails in CI, which is the intended default. No assertion was removed or loosened; one was added. On the same clean clone with `CI=1`, the suite now ends in 4.0 minutes: 40 passed, 0 failed, 10 not run - the eight tagged tests with their reason and the two `fixme` beats of ADR-0042 and P7.8. Left open: whether the eight tagged tests still pass is only shown by a laptop run with the city, and `/route` itself still reads "Loading the city's hospitals and chronic junctions" indefinitely when facilities answer 404 - a copy defect under 6.8, not fixed here.
- Date: 2026-09-13

## ADR-0044 Pulse's update is ES-MDA with perturbed observations, not a single stochastic EnKF step

- Context: CLAUDE.md 11.6's acceptance test asks for two blocked pipes in the top five with spread cut by at least 40 %. Under the single-step stochastic EnKF the ranking held at every seed, but the spread cut cleared 40 % at only 49 of 100 (observation seed x ensemble seed) pairs. Its median at the committed observation draw was 0.332 (0.083-0.462), so `test_the_observations_cut_the_blocked_pipes_spread` shipped `xfail(strict=True)`. Against an exact grid-Bayes posterior the old update under-cut the spread by a median 0.115. That was a filter defect, not a data limit: the exact posterior clears the floor at 10 of 10 observation draws (median 0.518).
- Decision: `assimilate()` runs `MDA_STEPS = 4` ES-MDA passes (Emerick & Reynolds 2013). Each pass inflates the observation variance by 4, re-evaluates H on the updated members and applies Appendix A's perturbed-observation update, with y + eps drawn afresh at the inflated variance. The prior ensemble is moment-matched: each edge's draws are standardised to zero mean and unit sd before scaling. Unchanged: N_e = 50, the hop localisation and reachable mask, MIN_SD, INFLATION (applied once, to the prior draw), the empty-batch early return and the public signature. `EnkfResult.notes` names the method.
- Alternatives: the square-root (Whitaker-Hamill) ES-MDA passes too (100/100) but drops 11.6's perturbed observations. Raising N_e to 200 members in one step measured a median of only 0.377. Leaving the test xfail was the status quo. Widening the 40 % floor is forbidden by rule 13.
- Consequence: measured on the repo function, both pipes reach the top five at 100/100 pairs. The cut clears 0.40 at 100/100 pairs for observation seeds 0-9 and 7-16 x ensemble seeds 2019-2028, and at 98/100 for 0-9 x 0-9 (worst single pair 0.352). The median over ensemble seeds clears 0.40 at every observation draw (0.457-0.564); the committed draw's median is 0.498 (0.406-0.538). Median over seeds, the posterior is within 0.017 of exact Bayes in mean and 0.027 in cut, and the test is green without xfail. Cost is four H evaluations per cycle: milliseconds with the analytic operator, 4x for a drain1d or Twin-linearised one. The narrower spread is the correct posterior, not a calibrated one: when the prior is wrong (0.20 +/- 0.12 against a truth of 0.85) the truth sits inside the 90 % interval about 40-46 % of the time, as it does under exact Bayes, so the drain map's beta +/- sd must not be read as coverage. Demo runs baked before this change carry single-step posteriors until the next re-bake.
- Date: 2026-09-14

## ADR-0045 Vendor the OASIS CAP 1.2 schema instead of fetching it

- Context: 7.5 AC2 needs a test that VARUNA's CAP validates against CAP 1.2, and the suite runs with VARUNA_OFFLINE=1. The task P8.7 was ticked on a "schema validation test" that did not exist: `xmlschema` was declared as a dependency and never imported.
- Decision: vendor CAP-v1.2.xsd byte for byte in `services/products/varuna_products/schemas/`, with SOURCE.txt holding the source URL, retrieval date (2026-09-14) and sha256; a test re-hashes it. `validate_cap()` returns named violations. `cap_xml` writes CAP times to whole seconds and refuses naive datetimes rather than assuming IST, because the schema restricts `sent`, `onset` and `expires` to `YYYY-MM-DDThh:mm:ss+hh:mm` and a cycle time with microseconds produced a value it rejects.
- Alternatives: fetch the XSD at test time (breaks offline, or skips silently); validate structure by hand (proves nothing about the standard).
- Consequence: 272 of 272 committed demo CAP documents validate and regenerate byte-identically; generated CAP validates in baked, replay and live modes, with Actual only in live. A schema update is a deliberate re-vendor with a new hash. Not covered: a UTC offset carrying seconds (a historical LMT zone) would still fail CAP's pattern; IST and UTC are unaffected.
- Date: 2026-09-14

## ADR-0046 Stage totals count each cycle stage once

- Context: `RunMeta.total_ms`, `CycleStatus.elapsed_ms` and `CycleLogEntry.total_ms` summed every `stage_ms` key. run.json carries the Twin's wall clock as `twin` plus five sub-timings measured inside it (`twin_total_ms`, `twin_surface_ms`, `twin_drain_ms`, `twin_coupling_ms`, `twin_hydrology_ms`), so the deployed status reported 189,704 ms for the 09:10 IST cycle whose stages took 76,851 ms. All seven demo cycles were inflated 2.3-2.7x.
- Decision: one named rule, `varuna_schemas.models.top_level_stage_ms()`, keeps only the keys in `CYCLE_STAGES`; `stage_total_ms()` sums them, and all three computed fields call it. The console's `totalStageMs` applies the same allowlist. Sub-timings stay in `stage_ms` as provenance. `packages/schemas/tests/test_stage_totals.py` pins the rule, including that an unknown sub-key or a `total` key does not inflate a total.
- Alternatives: a denylist of `twin_*` and `total` keys (fails open: the next sub-timing someone adds inflates totals again); dropping the sub-timings from run.json (loses the provenance that shows where the Twin's time goes); rewriting committed run.json files (unnecessary, and it would disturb byte-identical bakes).
- Consequence: the registry re-validates `RunMeta` on read, so `/v1/runs`, `/healthz`, `/v1/cycle/status` and the replay cycle log serve corrected totals without a re-bake: 64.8-147.2 s against the stale 155.1-359.3 s. Committed run.json files keep a stale serialised `total_ms` until their next re-bake. A new top-level stage counts only once it is added to `CYCLE_STAGES`.
- Date: 2026-09-14

## ADR-0047 The Twin hot start is forced by Sky's own analysis of elapsed radar frames

- Context: a hot-started Twin needs the rain that has already fallen, forced from what live mode would have (lead decision 4, 2026-09-14), and the bake ladder must not change results (rule 8: 06:40 baked via 06:10 must equal 06:40 baked via 06:35).
- Decision: `varuna_sky.analysis`. Each 5-minute step is labelled by the instant it ends at, the Twin's convention. It holds the newest radar frame at or before that instant, for at most one radar interval, and runs `run_sky`'s QC, Z-R and gauge merge on it with that frame's hour of history (so every gauge reading in the 60-minute fit window has a frame). Clutter QC is skipped, and labelled, until six frames exist. No state is carried between steps, so a step is byte-identical wherever a series starts, and `truth/rain.zarr` is never opened. It is not yet wired into the cycle (R4).
- Alternatives: interpolate between frames (needs the next frame, which is look-ahead); force on truth (contaminates verification and cannot run live); use the three frames `run_sky` uses (drops gauge readings older than about 25 minutes from the fit, and measured 23-38 % low at 08:40 and 09:10).
- Consequence: against the reconstruction's truth, AOI mean, the analysis carries 0.955 of the mass over 05:40-08:40 and 1.106 over 05:40-09:40, hourly 0.80-1.46. The good three-hour ratio comes from errors cancelling, not from each hour being right: the analysis trails truth by about one gauge interval, because gauges report every 15 minutes and the merge anchors on each station's newest reading. Holding 10-minute frames accounts for 0.2 % of the error. These ratios must be quoted beside any analysis-forced hot-start depth. Cost is about 24 ms per 5-minute step on the 522 x 323 grid.
- Date: 2026-09-14

## ADR-0048 The drain graph's gravity is audited before it is fixed

- Context: `check_connectivity` proves every node has a path to an outfall, not that the path runs downhill. CLAUDE.md 10.1 step 7 fixes invert depth at 1.5 m (3 m on trunks) and asks for a 0.3 % minimum slope; the blueprint (7.1) bounds inverts at 1-3 m below ground and resolves flat areas with a 0.2-0.5 % slope. Where terrain rises toward the outfall both rules cannot hold, and the build silently let the slope rule give way.
- Decision: add `varuna_city.gravity`, a `varuna city audit-gravity` command, and a hydraulic-connectivity row under the topological one in REPORT.md, with no change to how the graph is built. The regrade is a separate change with its own ADR.
- Alternatives: regrade immediately (digging down to hold 0.3 % reaches 39 m, far past the blueprint's 3 m bound); cap the depth without measuring what the cap leaves adverse.
- Consequence: measured on city/mumbai: 18,994 of 49,770 edges (38.2 %, 686 km) run uphill; 26,651 fall less than 0.3 %; 26,622 stored `slope` values disagree with their own inverts (the column reads at least 0.003 everywhere); 27,974 of 49,897 nodes (56.1 %) have an invert above their own street between them and their outfall (excess median 3.17 m, p90 8.76 m, max 31.97 m). 25 of 28 register points have such a node in their 5x5 window; at Hindmata it is 29 of 32, up to +8.56 m. The tidal outfalls MITHI and WORLI have inverts at 9.50 m and 5.72 m, above the 3.94 m tide maximum. REPORT.md's 100 % connectivity is topological; the hydraulic figure is 43.9 %.
- Date: 2026-09-14

## ADR-0049 The coupled run advances the surface through a run-long stepper, audited every 100 sub-steps

- Context: `run_surface` redid its setup on every one of 2,160 coupled calls: three isfinite passes, the sea index, a workspace, two full-grid volume sums and the notes. With the kernels stubbed, that setup measured 1.95-2.06 ms best per call on the Mumbai grid, against about 3.1-3.4 ms of kernels. Its every-100-steps mass audit never fired, because a coupled call takes one or two sub-steps.
- Decision: `swe2d.SurfaceStepper` builds the terrain, sea index and workspace once, and validates rain once per 5-minute step. It still checks both exchange rasters with an isfinite mask on every call (about 0.03 ms each), because the capture limiter reads a NaN inlet rate as "drain the cell" and would publish finite water. Mass is audited when every 100th sub-step completes, over the window since the last audit and over the run so far, and once more in `finish()`. Every audit comparison is written `not (error < TOL)`, so NaN raises; `run_surface` and `_audit` got the same fix. `run_surface` stays as the public API and the specification. `tools/profile_twin.py` is the reproducible profiler.
- Alternatives: keep the per-call setup (about 2 ms x 2,160 calls per 3-hour run); drop the exchange finiteness check (rejected: NaN passes `>=`); a sum or dot product instead of the mask (measured slower, 0.055 and 0.077 ms against 0.032 ms); a cumulative-only ledger (dilutes sensitivity to a transient leak).
- Consequence: h, qx, qy and the ledger are bitwise identical to the old loop at every sync, and `run_twin` outputs are identical, so no Twin version bump. Stepper overhead is 0.20-0.22 ms best and 0.29-0.36 ms median per call on a shared machine (12 python processes). A corruption is caught within 100 sub-steps (about 5 simulated minutes on Mumbai) rather than on the same call, still before the run returns. The drain kernel is now the largest cost: 13.2 s of a 27.1 s one-hour coupled run under contention, so section 11.3's 8 s for three hours is still missed.
- Date: 2026-09-15

## ADR-0050 The segment table is built column-wise and held bitwise equal to the shipped path

- Context: `depth.segment_forecast` took 8.5 s of section 11.8's 2 s products budget on the 08:40 cycle with 20 members: a percentile loop per segment plus 766,656 row dicts.
- Decision: `varuna_products.segment_table` groups segments by cell count so each group takes one percentile call, lays the frame out column by column with each non-numeric dtype inferred once from its distinct values, and renders the safe-until JSON once per distinct pattern. It is held to bitwise and exact-frame equality with the shipped path, including a rebuild of the baked 08:40 parquet. It is not yet wired into `depth.py`.
- Alternatives: a Numba percentile (drops bitwise equality with `np.percentile`); a pyarrow table build (different dtype inference).
- Consequence: on the real Mumbai index, sampling goes from 2.14 s to 0.20 s; the segment forecast from 6.67 s to 0.34 s for one member and from 8.50 s to 0.97 s for twenty, of which 0.62 s is the unchanged member statistics (best of N, 12-13 python processes). With it the products compute is 1.58 s excluding surcharge, and the writers section 11.8 also counts (36 rasters 0.99 s, parquet 0.45 s, segments_wet.json 0.57 s) take it to 3.59 s, so the budget is still missed. The `stage_ms['products']` timer excludes those writers and cannot be used to tick 11.8.
- Date: 2026-09-14

## ADR-0051 The products stage runs on the column-wise segment table, parallel raster encoding and a deduplicated wet-segment writer

- Context: ADR-0050 built `varuna_products.segment_table` and left it unwired. Section 11.8 gives products 2 s, and that includes the 36 depth PNGs, `segment_forecast.parquet` and `segments_wet.json`, which `stage_ms['products']` does not time: `twin_cycle.py` closes the mark before `_write` runs the writers. The PNGs were encoded one after another, and under cProfile `write_wet_segments` spent 2.9 s in 1.17 M `round()` and 935 k `_probability` calls.
- Decision: `depth.segment_forecast` keeps its signature and log line and composes `sample_segments`, `ensemble_statistics`, `safe_until_blobs` and `forecast_frame`. `write_depth_rasters` encodes on a thread pool of `min(RASTER_WORKERS = 8, cpu_count)` (a keyword-only `workers` overrides it) and writes the files in step order. `write_wet_segments` applies Python's `round` and `_probability` once per distinct float64 bit pattern and places the results by position. `depth.segment_points` goes through the mtime/size memo. The pre-change row loop is frozen verbatim in `test_segment_table.py`, and both the wired function and `segment_forecast_columnar` are held to it with `check_exact`.
- Alternatives: `np.round` (not the correctly rounded decimal Python writes, so files would drift in the last digit and break rule 8); a process pool for the PNGs (pickling cost for no gain, since zlib releases the GIL); deduplicating by float equality (merges -0.0 with 0.0; the test carries both, and NaN).
- Consequence: all 75 depth products of the 08:40 cycle are sha256-identical to 1bc503a and to the shipped bake, checked by the implementer over three rounds and independently by the reviewer, who added a float32 synthetic case; `test_idempotence` passes. On the 08:40 inputs with 12 falling to 10 python processes: segment forecast 6.86-8.46 s to 0.75-1.04 s, rasters 0.95-1.22 s to 0.27-0.36 s, `segments_wet.json` 0.89-1.27 s to 0.45-0.59 s, and `segment_points` 0.31-0.61 s on every call to 0.35 s on the first call and 0.001 s after. Compute plus writers fell from about 10.5-13 s to about 2.7 s warm and 3.6 s cold, so the 2 s budget is still missed; the largest items left are the segment forecast, `segments_wet.json`, the parquet write (0.39-0.47 s) and the surcharge product (0.32-0.35 s). A real bake of 08:40 records `stage_ms.products` = 3,009 ms, compute only, with 10 then 8 python processes. These timings predate ADR-0052's geometry join, which adds a one-time parse of about 1.1 s to the first cycle in a process; the two have not been timed together. `stage_ms` cannot be read against 11.8 until the timer covers the writers or `twin_cycle.py` records a separate writer key.
- Date: 2026-09-15

## ADR-0052 Reversed-flow edges carry their line from `city/<city>/map/drains.geojson`

- Context: M9 animates a dash along each reversed drain edge, but `DrainNetwork` carries no node coordinates, only each node's 30 m exchange cell, and 80-105 of the 500 stored reversed edges per demo cycle have both ends in one cell, so a line built from cell centres would have zero length or sit off the pipe the Drains layer draws.
- Decision: `surcharge.py` joins each stored reversed edge to the city's map export by `edge_id` and writes `path`, a list of [lon, lat] ordered from `from_node` to `to_node`, turning the line over when the export lists it the other way. That is the direction the graph assigns, not necessarily downhill (ADR-0048). An edge with no line, or whose line ends at other nodes, gets `path: null`, is counted in `n_reversed_stored_without_path` and gets a product note; no line is invented. The product records `reversed_edge_geometry: "map/drains.geojson"`, relative so two machines bake the same bytes. The export is parsed once per process, cached on resolved path, `mtime_ns` and size, and the city comes from the run id when no `city_root` is passed, so `twin_cycle.py` is unchanged. `/v1/nowcast/surcharge` appends the product's notes after the run's rather than replacing them. The tidal-first sort, the 500 cap and the counts taken before the cap are unchanged.
- Alternatives: a serve-time join in the API (collides with the attribution work in `routers/depth.py`); a client-side join against the 49,770-edge drains layer (loads it even when that layer is off); cell-centre lines (zero length on those edges).
- Consequence: 3,500 of 3,500 stored edges across the seven committed demo runs join to the raw export with their own orientation and no vertex mismatch (the implementer's test and the reviewer's independent script); mutation tests catch a join that never flips, accepts another pipe's line or invents one; a fresh bake carries a path on 500 of 500 stored edges and stays byte-identical. The product grows 11.6-13.0 %. The first cycle in a process pays 1,121 ms to parse the 18 MB export (a cold call 3,037 ms against a 1,030-1,445 ms baseline, 14-20 python processes); warm calls sit inside the baseline's spread. Nothing on screen uses it yet: `path` is absent on 500 of 500 stored edges in all seven committed runs and on the deployed runs seeded from them, so M9 draws nothing until the combined re-bake, and the layer panel says "This run stores no pipe geometry, so they are counted here but not drawn." The one tidal edge a fresh bake stores, MUM-E026920 at Mahim, does not survive the drain-graph fix in flight, whose scratch rebuild places no Mahim outfall (nearest node 1,049 m). A compact edge-to-line table written by the city export would remove the parse.
- Date: 2026-09-15

## ADR-0053 The surcharge pulse (M8) and the reversed-flow dash (M9) run on deck's animation clock

- Context: M8 was a React state loop quantised to 20 steps, costing 12.5 `CityMap` renders a second, and it drew a growing filled disc rather than a ring. M9 did not exist. deck.gl 9.3.11's `PathStyleExtension` scopes its dash offset inside an `if` in the fragment injection, so a phase cannot be added there.
- Decision: two `LayerExtension`s set a `phase` uniform in `draw()` from `performance.now()`. `SurchargePulseExtension` scales a second, stroked `ScatterplotLayer` ring by 1 + 1.4 x phase and fades it by 1 - phase over a static core. `FlowDashExtension` appends `vDashOffset += phase * (dash + gap)` to the stock `vs:#main-end`, which moves the dash toward `path[0]`, the from-node. DeckGL `_animate` is on only while a marker or a drawn edge is in view and motion is allowed. Tidal edges draw at every zoom, inland edges from zoom 14 and in view, and an edge with no path is not drawn. The dash reuses the 1.6 s surcharge period, because section 8 gives M9 no duration. The layer panel quotes the run's `n_reversed_edges` whether or not the layer is on, says the run reports no total when that field is absent rather than substituting the 500-capped stored count, and makes no causal claim: only 23.8-28.1 % of stored inland reversed edges run along adverse pipes, below the 38.2 % network rate (ADR-0048). Section 8's M8 row still names CSS keyframes on an `IconLayer` sprite as the implementation; rewriting that row is left to the lead.
- Alternatives: `TripsLayer` with repeating timestamps (not needed once the uniform worked on a real WebGL2 context); a React per-frame offset (repeats the render cost); CSS keyframes on a sprite or HTML markers, as section 8's M8 row says (does not scale to 500 manholes and leaves deck's canvas).
- Consequence: 0 React renders per animation frame (vitest), flipping the shader's sign fails 2 tests, and under reduced motion no pulse layer is built and the canvas is byte-identical 700 ms apart. The cost moved to the GPU: `_animate` redraws the whole scene while any marker is in view, which on the console is nearly always. Headed Chromium on the Iris Xe (D3D11), full `/console` at 1440 x 900, `next dev` build, 10-12 python processes from other agents contending: 43.7, 58.3 and 56.2 fps mean with the pulse on, 59.7 and 60.0 with the surcharge layer off, and 60.0 under reduced motion once settled. That is marginal against section 14's 55 fps, and neither that budget nor 7.2's frame-rate AC is ticked. Open decision: measure idle again on a production build with nothing else running; if it is still under 55 fps, draw the two loops on a second, small deck canvas so a pulse frame never redraws the city. No dash is drawn until the runs carry geometry (ADR-0052), and a tidal edge is about 1 px at the citywide fit, so the 1:40 beat needs a zoom to the outfall before the dash reads.
- Date: 2026-09-15

## ADR-0054 Traffic anomaly detection is vectorised, gains a windowed form, and Pulse's city joins are cached in process

- Context: section 11.6 gives Pulse 3 s. At 09:10 IST the per-segment Python loop in `detect_anomalies` took 6.3-9.4 s of `run_pulse` (19-21 python processes), and three joins over static city files - pipe geometry, street names and contributing area - were rebuilt every cycle, where caching them saves about 1.8-2.2 s warm. Carrying the posterior forward (P7.3) also needs detection over the window since the previous cycle, which the single-snapshot form could not give.
- Decision: `detect_anomalies` parses each distinct timestamp once, factorises segment ids in groupby order, takes sigma from pandas' grouped std over the same rows in row order (bit-identical to the loop), and gets persistence, count and minimum z from `bincount` and a grouped min; the latest row per segment comes from a stable `lexsort`. Section 11.6's rules are unchanged, and the old loop is kept verbatim as `_reference_detect` in `test_traffic.py`. `detect_anomalies(speeds, start=, end=)` scores every snapshot in (start, end] exactly as `at=` would; passing `at=` together with `end=` or `start=`, or passing neither `at=` nor `end=`, raises (the `start=` case since d207510). `citycache.cached` keeps one entry per join, keyed on the source files' size and `mtime_ns` plus a blake2b digest of the edge and node order; the unchanged helpers in `cycle.py` build the entries, and arrays come back read-only.
- Alternatives: vectorising `_edge_geometry` itself (NumPy rounding can differ from Python's `round()` in the last ulp, which breaks rule 8); a disk cache (another artifact to invalidate); a stage-budget test with a fixed ceiling (flakes or has to be widened under contention, so none was written).
- Consequence: anomalies, observations, disagreements, notes, the beta hashes and the drain-health hash are identical at all seven demo cycles, by the implementer's golden harness and by the reviewer's own script against `git show main:`; the 62 Pulse tests pass. Wall clock, old and new code interleaved in one process: at 09:10 with 19-21 python processes the old code took 13.67-16.64 s and the new 6.18 s cold, then 3.56 and 4.97 s warm; at 08:40 with 15-17 processes, 11.52-14.75 s old against 6.51 s cold and 2.15-2.16 s warm. The reviewer measured 09:10 with 10 processes at 1.47-1.65 s warm against 5.75-5.96 s for the old code, and did not measure cold. The budget is met warm at the lower process count and missed cold; no cold figure exists without other agents running. What remains warm is `assimilate` (1.12-2.45 s) and `drain_health` (0.85-1.37 s), measured at 19-21 processes. Nothing calls the windowed form yet. Where a feed carries duplicate rows for one segment and snapshot, the new selection always keeps the later row; the old quicksort kept it only while a group was small, and the demo feed has no such rows. No regression guard pins the speed-up.
- Date: 2026-09-15

## ADR-0055 The tide stage is read in the DEM's frame through `manifest.tide_datum`

- Context: `tide.csv` for MUM-2019-07-02 is anchored to the 4.92 m civic high-water statement, which names no datum and is taken as a height above chart datum. The Twin's terrain is Copernicus GLO-30, whose heights are relative to the EGM2008 geoid per its product handbook (https://dataspace.copernicus.eu/sites/default/files/media/files/2024-06/geo1988-copernicusdem-spe-002_producthandbook_i5.0.pdf). The solver compared the two with no conversion, so the sea at the tidal boundary stood high by the whole chart-datum-to-mean-sea-level offset.
- Decision: `tide.csv` stays in chart datum, as sourced. `manifest.tide_datum` records `stage_datum: chart_datum`, `msl_above_chart_datum_m: 2.70` with range [2.66, 2.73], the derivation, both PSMSL station 43 URLs (https://psmsl.org/data/obtaining/stations/43.php, https://psmsl.org/data/obtaining/rlr.annual.data/43.rlrdata), the DEM datum with its source, and a residual that says the EGM2008-to-local-MSL separation is not quantified. Derivation: RLR(1964) is 13.0 m below BM 2(PP)(1), which is 8.522 m above chart datum, so RLR = chart datum - 4.478 m; annual mean sea level of 7,141 / 7,154 / 7,207 / 7,211 mm RLR in 2015 / 2017 / 2020 / 2024 is 2.663 / 2.676 / 2.729 / 2.733 m above chart datum; 2019 has no annual value and interpolates to 2.711 m; 2.70 m is used. `varuna_twin.city.load_tide` subtracts the offset once for a chart-datum stage and records a `datum_note` that the cycle adds to the run notes. The `tide.stage` bus event keeps the sourced `stage_m` and adds `stage_datum`, `datum`, `offset_to_dem_m` and `stage_dem_frame_m`. A bundle with no block is read as written. The validator stays at 13 rules: B9 adds a note when `tide.csv` has no `tide_datum`, and an error when a block has no `tide.csv`.
- Alternatives: rewrite `tide.csv` in mean sea level (it would stop matching its source); label the mismatch and leave it (it is a sourced correction to a wrong input); convert in the replay clock as well (the bus keeps the sourced stage and carries the converted one beside it).
- Consequence: inside 05:40-09:40 IST the 17 stage rows run -2.655 to +1.236 m in the DEM frame instead of 0.045 to 3.936 m. Regenerating MUM-2019-07-02 leaves 95 of 96 files byte-identical, only `manifest.json` changed; both design storms are 73 of 73 identical and carry no block; a cycle baked twice on the new manifest is still byte-identical. Caveats that travel with the number and are not yet in the manifest: PSMSL's station notes record that the benchmark changed in 1966 to BM 5/88, 7.44 m above chart datum, and the 2020 and 2024 annual values carry the `Y` flag in the RLR file. `stage_reference` also carries a second assumption, that the civic statement's datum is the Apollo Bandar chart datum PSMSL documents. `varuna city audit-gravity --bundle` still reads the raw chart-datum maximum (`services/city/varuna_city/cli.py`) against DEM-frame inverts, so it now disagrees with the Twin by 2.70 m. Every baked run predates the conversion. The reversed-flow count at the Mahim outfall with and without the shift (R3) was not measured, because this change did not run the Twin, and the drain-graph fix in flight places no Mahim outfall at all in its scratch rebuild (nearest node 1,049 m). The Phase 4 exit's reversed-flow edge at a tide-locked outfall must therefore be re-measured on the regraded graph, with this conversion, before it is claimed. A Twin checkpoint's fingerprint should carry this block (ADR-0056).
- Date: 2026-09-15

## ADR-0056 The Twin resumes from a `TwinState`, and its mass balance divides by this run's inflow

- Context: every cycle cold-starts the Twin - a dry surface, drains at invert, fresh SCS accumulators - so an 08:40 forecast ignores the rain that has already fallen (ADR-0047 builds the forcing a hot start would catch up on). The storm-realism plan's denominator for a resumed run, max(in, stored_start), would relax section 11.3's 0.1 % budget by the ratio of carried to new water, which is the late-storm case the audit most needs to see.
- Decision: `TwinInputs.initial_state: TwinState | None` and a required `TwinResult.final_state`. The state holds surface h/qx/qy, drain heads and flows, pump and tank filled volume (empty today), the depression store and cumulative rain, `valid_ts` and a `TwinFingerprint`: grid shape, transform, CRS, node and edge counts, sha256 of `z_invert` and of the blocked mask, and an optional provenance mapping. A mismatch, or `valid_ts != t0`, raises and names every difference; outfall heads are still pinned at the resume t0; a resumed state's arrays are copied, and `final_state` arrays are read-only. The coupled error is |(stored_end - stored_start) - (in - out)| / V_in, with V_in this run's own inflow; at or below 10 m3 of inflow the residual is held to `MASS_BALANCE_MIN_RESIDUAL_M3` = 0.01 m3, the relative budget at its own threshold.
- Alternatives: max(in, stored_start) as the denominator (a test shows it hides 0.5 % of injected rain under 4,424 m3 of carried storage); skipping `pin_boundaries` on resume (carries a stale boundary under a changed tide); leaving sink state out until pumps are wired (forces a checkpoint format change later).
- Consequence: 6 steps plus a 6-step resume are array-equal to 12 steps with a free outfall and under a rising tide, re-derived by the reviewer at a maximum difference of 0.0. With no `initial_state` the runner is byte-identical to 1bc503a on five fixtures; four digests are pinned, but that test runs only where Numba targets tigerlake, so CI relies on the explicit-cold-state test. A Mumbai state is 7.54 MB and its fingerprint costs 0.71 ms (14 python processes). Nothing in the cycle uses it yet: `services/cycle` and `services/api` never pass or read a state. Four things are open before it is wired. The surface stepper's own audit (`swe2d._check_ledger`, `SurfaceRun.error_fraction`) still divides by initial + in, so on a hot start the surface-only check is relaxed by carried water while the coupled one is not. The fingerprint hashes neither drain topology nor DEM heights, so a rebuilt network with the same counts and inverts would resume; the cycle's provenance should carry a city build hash and the tide datum. Sink volume is carried but counted on neither side of the ledger. The 0.01 m3 floor has not been measured on a Mumbai drain-down with no new rain, where it may breach, and `run.json` records only `mass_balance_err`, which reads 0.0 in that branch, so `residual_m3` and `ok` must be written too. The floor also changes the cold path in one case: a cold run with 10 m3 or less of inflow can now report `ok` False, which no fixture exercises.
- Date: 2026-09-15

## ADR-0057 An alert is new when its scope, place and level are new, and the phone mock's chime is synthesised

- Context: motion M16 slides a new alert into the queue and pops the phone mock. Alert ids embed the run id, so two cycles share none of them: diffing ids would slide all 60 cards in on every change of cycle. Section 7.5 also asks for a soft sound, muted by default and on for the demo, and a browser refuses to start audio that no gesture unlocked.
- Decision: identity is scope plus hotspot id (or area) plus level (`lib/alert-identity.ts`); the first queue a screen shows is not news; cards are keyed by identity, so a street still warned about at the same level keeps its card and glides while new ones slide 12 px over 180 ms. The chime is two sine tones synthesised with the Web Audio API (`lib/sound.ts`), played only while the settings toggle is on and never under reduced motion; its audio context is created on the first pointerdown or keydown after sound is switched on, so the click that changes the cycle unlocks it.
- Alternatives: diffing by id (every card new every cycle); an audio file (a download, and one more asset for the offline pack); creating the context at the first chime (suspended by the browser, with a console warning that breaks section 14's zero-warning rule).
- Consequence: moving the picker from 06:40 to 08:40 slid 57 of 60 cards in, shook the phone once (peak 3.98 px) and chimed once per batch; under reduced motion nothing translated, shook or sounded (next dev, measured by the implementer and passed by review). Several segments of one street that share an area count as one identity. The shake loses frames to 170-440 ms long tasks when 60 cards commit in a development build; a production build has not been traced.
- Date: 2026-09-15

## ADR-0058 The verification sweep is scored once per set of inputs

- Context: `GET /v1/verification` recomputes the whole sweep - every run's wet streets routed against the city graph - on each request: 9-10 s warm and 67.6 s under load on the laptop. The landing page asked for it, and since 2026-09-15 so does every screen's top bar (P0.7), which put a CPU-bound computation on every page load of a shared 1-CPU host.
- Decision: the router keeps the sweep's answer per event, keyed by the size and modification time of the bundle's `ground_truth.geojson` and of every run's `segments_wet.json`, and recomputes when any of them changes. One lock makes concurrent first requests wait for a single computation. The deployed entrypoint asks for the score once, 45 s after the API starts, so the first visitor reads a kept answer.
- Alternatives: caching in the browser only (every visitor still starts a sweep); a time-to-live (a new bake would be served a stale score); precomputing at bake time into each run (a score is per event, across runs, not per run).
- Consequence: a second request with unchanged inputs costs a directory listing; a new or rewritten run is scored again (three tests in `services/api/tests/test_verify_cache.py`). The kept answer lives in the API process and is recomputed after a restart. While the first computation runs, the chip shows the committed copy labelled as such.
- Date: 2026-09-16

## ADR-0059 Google Maps is the citizen basemap, VARUNA draws over it, and the key refuses localhost

- Context: `PRD.md` 3.1 wants the citizen dashboard on the map a person in Mumbai already knows - Google's tiles, labels and gestures - with VARUNA's water and routes over them, while the operator console keeps its own dark Esri-and-deck renderer. The user supplied a browser key for the Maps JavaScript API.
- Decision: the dashboard loads Maps JS through `@vis.gl/react-google-maps` and puts deck.gl over it with `GoogleMapsOverlay` in **overlaid** mode (`interleaved: false`), because Google's WebGL2 context has no multisampling and interleaved paths alias. The dark styling is a `styles` array built at runtime from `tokens.json` through `cssVar`, so no raw hex enters `apps/command` and the map matches the depth ramp exactly; that forces a raster map with no `mapId`, since a `styles` array is refused on a vector map and no cloud Map ID exists for this project. Routing, geocoding and place search stay VARUNA's - measured refusals, and the point of the product.
- Alternatives: a vector map with a cloud Map ID (needs Console access this build does not have, and moves our colours into someone else's dashboard); interleaved deck (aliased every route line); Google Directions for the citizen route (the key refuses it, and it knows nothing about flood depth).
- Consequence: **measured 2026-09-19, the key carries an HTTP-referrer restriction that lists neither development origin**: `http://127.0.0.1:8899/probe.html` and `http://localhost:3000/probe.html` both answer `RefererNotAllowedMapError` from Maps JS v3.66.4d, with `google.maps` loaded and zero tiles drawn. So the fallback is the path that actually runs today: with the key absent, refused, or silent for 4 s, the dashboard renders the existing `FloodMap` - VARUNA's own Esri-and-deck renderer - with a labelled notice, and no console error escapes. Nothing on the screen depends on Google being reachable. The key must never be committed; it lives in `apps/command/.env.local` (git-ignored) and in Vercel. Until the team adds `https://varuna-dhrishta.vercel.app/*` and `http://localhost:3000/*` to the key's referrer list (`TASKS.md` D-25), the Google basemap is code that is built, tested and dormant.
- Date: 2026-09-19

## ADR-0060 Route spreading is a stated policy, not a traffic model

- Context: if every citizen asking for a safe way home is handed the same corridor, the safe corridor becomes the next jam - the failure mode a single-answer router creates by being used. VARUNA has no live traffic counts, so it cannot know demand.
- Decision: `POST /v1/route` returns up to three corridors, each genuinely different and each under the vehicle's depth threshold, with a share derived from each corridor's own spare capacity under the router's `phi(h)` slowdown. A request is assigned to one deterministically, by hashing its `trip_id`, so the same trip keeps its corridor across refreshes and a thousand trips land within 2 % of the stated shares. Spreading is offered, never forced: the reader sees all three and may pick. The screen carries the disclosure verbatim - the split is our policy, not a measured traffic count - and the response carries the same sentence in `notes`.
- Alternatives: one safe answer for everyone (creates the jam); a traffic model (there is no demand data, and inventing one would be a fabricated number under rule 6); random assignment per request (the same trip would flicker between roads on every refresh).
- Consequence: the mechanism is real and the demand is not, and both are said out loud. `trip_id` is the client's own random id, stored against nothing. A city with only one safe road returns one corridor and no disclosure, because there is nothing to disclose.
- Date: 2026-09-19

## ADR-0061 Live weather degrades to a stamped copy, and is never an input to a product

- Context: the citizen dashboard needs one true number the moment it opens, before any baked run means anything to the reader. Open-Meteo answers for Mumbai with no key (CC BY 4.0), but the venue may have no network, the offline package runs with `VARUNA_OFFLINE=1`, and an upstream outage must not take down a screen that is mostly about our own forecast.
- Decision: `GET /v1/weather` proxies Open-Meteo through the new `varuna_schemas.net` helper that ADR-0006 mandated and nobody had written, keeps a 15-minute in-process cache and writes the last good response to `data/cache/weather-<city>.json`. A cache hit, `VARUNA_OFFLINE=1` with a copy, and an upstream error with a copy all answer 200 with the copy's real `age_s`, `stale`, and a note saying why. Only a first request with no copy at all answers 503, naming the cause. The weather is context printed beside the forecast; nothing mixes it into a run, a product or a route.
- Alternatives: fetching from the browser (attribution then depends on each client, the free tier is spent per visitor, and there is no fallback when the network is gone); failing with 503 on any upstream error (a dashboard that loses its header because someone else's server blinked); serving the copy with no age (a stale sky presented as live, which rule 6 refuses).
- Consequence: the header always has something true to print and always says how old it is. Measured on the laptop by the implementer: 1,260.7-1,465.0 ms uncached, about 1.1 s of it Open-Meteo, and 5.2-21.8 ms cached, so the wire is touched once per fifteen minutes per city. The coordinates come from `CityConfig.bbox.center`, so a city works the day its config exists, and the response states `grid_point` and `grid_offset_km` because the answer is for a model cell and not for the point. A test replaces five socket entry points with tripwires and proves the offline refusal opens none of them.
- Date: 2026-09-19

## ADR-0062 An authority edit is an append-only overlay applied at read time, never a rewrite

- Context: a ward officer needs to close a street, move a pump, dispatch a plan and acknowledge an alert, and every one of those has to reach the router within seconds. Rule 8 says two bakes of the same inputs are byte-identical, and the offline package ships those bytes; a screen that edits a product would break both. The deployed API is also a public URL with no accounts.
- Decision: writes append a record to `data/ops/<city>.jsonl` and nothing else. `varuna_route.ops_overlay` reads it at request time, so the next route honours a closure without a product changing; the road-conditions feed reads the same overlay. Every write is gated behind `VARUNA_OPS_PASSPHRASE` and an `X-Varuna-Ops` header - unset means refuse, and the refusal names the variable - with thirty writes a minute per process. The screen splits what an engine consumes from what is only recorded, and says which it did after every action.
- Alternatives: rewriting the run's products (breaks rule 8 and the offline package); a database (infrastructure on stage, and the prototype's store is files by ADR of Phase 0); no gate at all (a public, unauthenticated write surface); real accounts (not a prototype's job, and a fake login would be a lie about what protects it).
- Consequence: the sha256 of every file in a baked run is unchanged after a closure, a pump status change and a dispatch, and a test asserts exactly that rather than arguing it; a second test closes a street and then routes over it, showing the route avoid it with the closure as its reason. The rate limit is a module-level deque, so with more than one worker the effective limit is thirty per worker, and nothing in the response says which process answered. `GET /v1/alerts` does not yet reflect an acknowledgement - the alert list is served from a router another task owns - so the ops log is the record until that is wired. A passphrase is a gate, not authentication, and the screen says so in those words.
- Date: 2026-09-19

## ADR-0063 The Rust router is a port, proved by parity, and its contraction hierarchy serves only the dry search

- Context: CLAUDE.md 11.9 names "a Rust Axum service with contraction hierarchies" whose contract is identical to the Python router's. Two implementations of one answer are the classic way to ship a quiet disagreement, and section 14 gives a route 300 ms p95 - which the Python router meets on the demo trip (62-73 ms) but strains on a cross-city one (207-230 ms, and 1.1 s under contention).
- Decision: `services/route-rs` ports `router.py`, `spread.py`, `reasons.py` and the ops overlay line for line, including CPython's float floor division, and reads the same inputs through an exported graph rather than re-deriving them. A parity test starts the binary and compares it with `plan()` leaf by leaf on 31 trips across all seven profiles, six cycles, a dozen trip ids and three closures, excluding only `ms`, with no tolerance. Contraction hierarchies are built for the **naive, dry-weather** search only, where the cost does not depend on time and CH is exact; the VARUNA search and its two alternates stay time-dependent Dijkstra, and `/healthz` names which engine serves which.
- Alternatives: claiming CH for the whole router (it is not exact once the cost depends on arrival time, so saying so would be a false claim about the method); a fresh implementation rather than a port (two routers that were never the same answer); comparing with a tolerance (which is how a real disagreement survives a test suite).
- Consequence: 72 of 72 parity checks pass. KEM to Sion p95 2-3 ms against Python's 62-73 ms; the cross-city trip 10.6-13.7 ms against 207-230 ms; graph load 19-21 ms against 1.7-2.7 s. CH pays inside the naive search (0.062 ms a query against 1.618 ms) and barely moves a whole request, because three of the four searches are time-dependent. The port is a second implementation to keep in step: it drifted the day it merged, when the pedestrian hazard rule landed in Python, and the parity test caught it rather than the demo. The deployed API still serves the Python router.
- Date: 2026-09-22

## ADR-0064 The IMD decoder reads the image's own legend, and refuses an image it does not recognise

- Context: task P2.9. IMD publishes Doppler radar products for Mumbai as images, and the prototype's radar is otherwise a storm-designer reconstruction (CLAUDE.md 3.2). A decoder that guesses at colours it does not know would put invented reflectivity into the same pipeline as the real thing.
- Decision: `varuna_sky.decode_imd` reads the colour bar out of the image it is decoding rather than carrying a hard-coded palette, georeferences from the radar site plus a concentric fit of the range rings, and maps each pixel to the legend's own dBZ classes. A pixel whose colour is not in that legend - a spoke, a ring, a coastline, a label - is masked as unobserved, never interpolated. An image whose layout does not match the one product described (`MUMBAI_COLABA_PPI_Z`) is refused rather than decoded approximately.
- Alternatives: a fixed palette (IMD re-renders its products, and the first re-render would silently shift every class); interpolating across the overlays (invents reflectivity exactly where the image has none); accepting any layout (a Veravali frame decoded with Colaba's geometry is wrong by tens of kilometres).
- Consequence: measured on one archived image, 1,053 of 14,400 Sky cells (7.3 %) are unobserved because every pixel in them was an overlay, and they are reported as such. P2.9 says "5-dBZ classes"; the Colaba legend has four, and the decoder outputs the legend's rather than re-binning. Nothing fetches IMD images on a schedule and `run_sky` does not call the decoder: Sky needs three frames ten minutes apart, this is one image, and `qc.coverage_mask` still assumes the radar sits at the domain centre while Colaba is 19 km from it. No attenuation, beam-height or PPI-to-CAPPI correction; slant range is taken as ground range. The image is cached, never committed - it is IMD's.
- Date: 2026-09-22

## ADR-0065 3D mode drapes the depth raster over a heightmap exported from the city's own DEM

- Context: task P6.15 asks for deck.gl's `TerrainLayer` with the depth raster as its texture. `TerrainLayer` wants an elevation image, and the city pipeline holds its conditioned DEM as a GeoTIFF a browser cannot read.
- Decision: `varuna_city.terrain_export` writes that DEM as a Terrarium-encoded PNG (323 x 522 for the Mumbai AOI), the API serves it, and the console drapes the current step's depth PNG over it at 2x exaggeration and 55 degrees of pitch. The export is deterministic: a re-export is byte-identical, and the round trip through the encoding loses 0.00195 m.
- Alternatives: a third-party terrain-tile service (another network dependency at the venue, and a different DEM from the one the physics ran on); building the mesh in the browser (the elevation still has to get there).
- Consequence: the terrain the operator tilts is the terrain the Twin solved on, exported in 0.72 s. Section 8 has no row for a camera tilt, so the toggle cuts to 55 degrees rather than flying there, in both motion modes - adding that row is a spec change, not an implementation detail. 3D measures 50.4 fps (31.3 under contention) against section 14's 55, so it stays off by default and the flat map is what the demo runs on.
- Date: 2026-09-22
- **Superseded 2026-09-23 by ADR-0069.** The heightmap and its exporter still exist and the API still serves them; the console's 3D *mode* no longer uses them.

## ADR-0066 The offline public map is built from VARUNA's own OSM layers, not from cached third-party tiles

- Context: task P9.10 wants the public map installable and useful with no network. The console's basemap is Esri aerial imagery, which is explicitly not redistributable (P10.6 keeps it out of the offline package), so caching it for offline use is a licence breach rather than a shortcut.
- Decision: `varuna_city.basemap_tiles` builds a PMTiles vector basemap from the city's own OSM extract - the roads, water and buildings the pipeline already holds - the API serves it, and deck.gl draws it. A service worker scoped to `/map` and `/report` caches the app shell, that basemap and the last forecast the map loaded, and answers an offline `POST /v1/reports` with 202 `{"queued": true}`, holding the report in IndexedDB until the connection returns. OSM is ODbL and carries its attribution on the map.
- Alternatives: caching Esri tiles (not ours to cache); shipping a third-party PMTiles extract (a large download and another licence to check); no offline map at all (the one screen a citizen opens in a storm is the one most likely to have no signal).
- Consequence: with the API stopped and the browser offline, /map draws the cached forecast over the vector basemap and says how old that forecast is, and a report submitted offline arrives when the connection returns - both driven in a browser rather than argued. The basemap is built by a standalone command (`make city-basemap`), so a deploy or an offline package that has not run it serves none; the cache version falls back to "dev" where no build id is set; and the offline water fill is deliberately quiet, which also makes it faint.
- Date: 2026-09-22

## ADR-0067 Hindi and Marathi ship as drafts, and say so

- Context: task P9.9 puts the public map and the report flow in English, Hindi and Marathi. The translations were written by the model that built the screens, with no native speaker on hand.
- Decision: ship them, with a glossary that keeps the product's own terms consistent across all three, tests that assert key parity, Latin numerals and no Devanagari digits, and a SIMPLIFICATIONS row saying the text awaits review by a native speaker. The honesty labels are translated as statements and never softened. Components shared with the operator screens fall back to English with no provider mounted, so /dashboard and /console cannot be broken by a locale.
- Alternatives: English only (the public map is for people who do not read English first); claiming reviewed translations (a claim nobody on the team has checked).
- Consequence: a Marathi reader gets a usable page today and the team knows exactly what still needs a human. With no locale routing the server always renders English, so a returning reader sees one English frame before the stored choice loads, and page metadata stays English.
- Date: 2026-09-22

## ADR-0068 An alert is raised across cycles, and a real send needs its keys

- Context: CLAUDE.md 11.10 raises an alert at P >= 0.6 for **two consecutive cycles** and clears it at <= 0.3. The product read that rule along the forecast inside a single cycle, which is a different statement, and its docstring said so. Section 7.5 also wants a real channel behind environment keys, and an escalation matrix that `config/escalation.yaml` never held.
- Decision: `write_alerts` reads the previous run's alert record from the registry (same city, no more than 60 minutes earlier) and holds a level until two consecutive cycles agree, clearing at 0.3, per situation and per level. `config/escalation.yaml` names five tiers and every alert carries the tiers its level reaches. `notify.py` holds a Twilio adapter and a WhatsApp Cloud adapter, each active only when all of its keys are set; a send counts as sent only on a 2xx carrying the provider's message id; every attempt is appended to a delivery log; and "Send to my phone" renders only where a sender is configured.
- Alternatives: keeping the within-cycle reading (it is not the rule the spec states); a mock that reports success (the one failure mode an alerting system must not have); committing keys to make it demonstrable (never).
- Consequence: the rule matches the spec, at the cost that the first cycle of any bake raises nothing, because two consecutive cycles means two. The 0.3-0.6 hold band is implemented and tested but never exercised on real runs: alert probabilities are still 0 or 1 from the deterministic Twin series, so the ensemble's exceedance is not yet what raises an alert. No real WhatsApp or SMS has been sent - there are no keys on this machine - and the adapters are tested against a fake transport only.
- Date: 2026-09-22

## ADR-0069 The console's 3D ground is Google's photorealistic city, and it is off by default

- Context: task P6.15 shipped 3D as the city's own DEM, Terrarium-encoded, with the depth raster draped on it (ADR-0065). It measured 50.4 fps against section 14's 55 and, more to the point, a grey mesh is a poor answer to "which street is this" - the one question a 3D view is for. Google's Photorealistic 3D Tiles are the same city, photographed.
- Decision: `layers/photoreal.ts` draws Google's tileset through deck.gl's `Tile3DLayer` with `operation: "terrain+draw"`, so every VARUNA layer drapes onto it through the same `_TerrainExtension` the heightmap used; `layers/terrain.ts` is deleted and its three still-needed exports move over as `SURFACE_EXTENSION`, `onSurface` and `hiddenLayers`. `onSurface` keeps the depth raster and drapes it, where `onTerrain` dropped it - Google's tiles are a photograph that knows nothing about the storm. The key travels as an `X-Goog-Api-Key` header, never in a URL. Draco and Basis decode on workers copied out of `node_modules` into `public/workers/` by `scripts/copy-loader-workers.mjs`, wired into `pnpm dev` and `pnpm build`, because loaders.gl's default worker URLs are a CDN and CLAUDE.md 17 requires the finale to run with the venue's network off.
- Alternatives: keeping the heightmap (it is ours and it works offline, but it cannot show a building); `Map3DElement` from the Maps JavaScript API (it renders the same tiles but will not host deck.gl's layers, and every VARUNA product would have to be re-expressed in Google's primitives); interleaving deck into Google's context (no multisampling, so every 1.6 px street aliases).
- Consequence: the operator tilts into a photographed Mumbai with the run's own water on it, and Mumbai has coverage to a **2.006 m geometric error** at Hindmata junction, traversed from the global root through six nested sub-tilesets on 2026-09-23. The cost is the frame rate and it is worse than what it replaced: **25.2-26.1 fps at street level and 11.4-11.9 fps at the AOI fit**, against 44.9 fps with 3D off and a budget of 55. So 3D stays **off by default**, exactly as ADR-0065 concluded, and the flat map is what the demo runs on. Three further costs are real and stated rather than solved: 3D now needs the network, where the heightmap did not; it needs a Google Cloud project with billing linked and the Map Tiles API enabled, which took three projects to get right on 2026-09-23 and whose failure mode is a bare 404 that names nothing; and Google's licence requires the tiles' own `asset.copyright` strings, aggregated and de-duplicated, on screen whenever they draw.
- Date: 2026-09-23

## ADR-0070 The drain X-ray draws the pipes where they are, and turns the depth test off to do it

- Context: the point of a 3D ground is not the buildings, it is being able to look under them. CLAUDE.md 7.3 wants the inferred drain network read street by street, and `drain_edges.parquet` has carried `z_invert_up_m` and `z_invert_dn_m` for all 49,770 edges since Phase 1 - but `varuna_city.export`'s allow-list dropped both, so no screen had ever been able to draw a pipe at the depth it sits at.
- Decision: the export keeps the two invert columns and the slope; `layers/drains-3d.ts` draws each pipe as a 3-component path with z interpolated along the edge by cumulative distance rather than by vertex index, in metres above mean sea level - the same frame Google's tiles use - with manhole shafts rising to the street from zoom 15. Depth exaggeration defaults to **1**: the honest answer is that 1.5 m of cover is shallow. The X-ray fades Google's surface to 20 % over `DUR_MS.crossFade` (motion M28), and in 3D every pipe is cloned with `depthTest: false`.
- Alternatives: exaggerating the depth so the network reads more dramatically (it would be a picture of a sewer that does not exist); drawing the pipes flat on the ground as the 2D layer does (that is the drain map we already had); cutting a hole in Google's mesh (deck gives no such operation, and a photograph with a hole in it is not more honest than a transparent one).
- Consequence: the operator can follow a trunk under a street they recognise. The `depthTest: false` clone is not a style choice - drawn straight, the pipes lose the depth test to Google's opaque mesh and vanish as the tiles refine over them, which is the one thing an X-ray must not do. Two honesty costs are carried in the panel's own words rather than hidden: every pipe is **inferred** from roads and terrain and is drawn dashed for it, and **18,994 of 49,770 edges run uphill** on the current graph (ADR-0048), which this view makes visible for the first time. The invert datum is the DEM's, so a pipe's depth is only as good as a 30 m open DEM's ground.
- Date: 2026-09-23

## ADR-0071 Attribution moves onto drain1d on the junction's own catchment, ranked by magnitude with the sign kept

- Context: ADR-0042 retired attribution after measuring that Flash-lite's `simulate` is element-wise per segment, so cleaning any pipe not under a street moves it by exactly zero. It named the fix, "put drain1d in the attribution loop", and priced it at 21,296 candidates times a three-minute coupled run. Both halves of that price were wrong: section 11.7 asks only for pipes within five upstream hops (51 at Hindmata, 0-90 across the register), and section 11.6 already describes the cheap operator - `drain1d` with the surface inflows frozen from the last Twin run - on a local network of 9-233 nodes rather than 49,897.
- Decision: `varuna_flash.whatif.attribute_pipes` cuts the junction's catchment out of the city graph (five hops up, down to a real outfall), freezes the street depth at the Twin's, runs every candidate as a disjoint copy of that network in one solver call (equal to one at a time at full precision), and reads how much more water the drain takes off the junction's cells by its peak. It ranks by **magnitude and keeps the sign**, because at a surcharging junction the pipe feeding it moves it the wrong way when cleaned and a reductions-only ranking would discard the pipe that explains it best. `rank_hotspots` runs it for the ten worst junctions above 5 cm, cleaning at this cycle's Pulse posterior, and every entry carries an `attribution_status` - ranked, refused, not_attempted, unavailable or off - so an empty list is never read as "no pipe is responsible" when nobody looked.
- Alternatives: reductions only, as 11.7 words it (discards the explaining pipe, above); one solver call per candidate (0.36 s each, about eleven minutes over the register); a longer sync interval (six times cheaper, and it changes the answer - Hindmata's capture 1,193 to 815 m3); keeping the refusal (it was a property of the emulator, not of the question).
- Consequence: measured on the 2026-09-26 bake. **Hindmata refuses at every storm cycle** - 51 pipes re-run, the best moves it 0.015-0.027 cm, under the 0.10 cm floor - because at that depth its inlets, not its pipes, limit the drain, which is also what ADR-0048's uphill inverts predict. Four junctions name pipes at 08:40 (Sion Subway 1, Sion Circle, Sion pedestrian subway, Dadar TT), and at three of them **cleaning makes the junction deeper**: fourteen pipes take Sion Subway 1 from 9.6 to 11.6 cm, because it is already surcharging and clear pipes above it deliver more water than the cleaning drains. At 08:10 Sion Circle's fourteen all point the right way and together move it 0.09 cm. So section 7.2's "cleaning these 14 pipes: 55 to 20 cm" does not exist on this model at any cycle, and the demo does not claim it; what the drawer can show is a measured answer the old emulator could never produce, including the uncomfortable one. Cost is the miss: attribution is 57.9-60.9 s of the products stage on a storm cycle against section 11.8's 2 s for the whole stage, recorded as its own `products_attribution` sub-timing; a bake pays it, a live cycle would too.
- Date: 2026-09-26

## ADR-0072 The coupled mass-balance failure was two exchange leaks, and the tide datum only revealed them

- Context: the 2026-09-23 re-bake measured 0.127, 0.535, 0.144, 0.260, 0.306, 0.204 and 0.310 % against section 11.3's 0.1 %, where the 2026-09-13 bake read 0.014-0.215 %. Four changes had never run together (ADR-0039, -0044, -0052, -0055). A single-variable bisect on the 08:40 cycle, rain cube cached so only the tide moved, reproduced both bakes bit for bit: 0.0020418 with ADR-0055's tide, 0.0007026 with the tide put back to chart datum. The residual moved 3.8 % while the denominator fell 64 %, because a sea 2.70 m too high had been admitting 5.35 Mm3 the city should never have received. ADR-0039, -0044 and -0052 cannot reach the Twin, which is given only terrain, network, rain, start time and tide.
- Decision: treat the regression as a reveal. `MassBalance` now splits the residual into the surface's own, the drain's own and the two exchange gaps (`twin.run.ledger`, and `mass_balance_ledger` in `run.json`). That put both solvers at exactly 0.0 and all 6,064.7 m3 in the exchange, in two places. The runner handed the surface the surcharge `compute_exchange` requested while `drain1d.step`'s supply check applied a scaled-down one; the runner now steps the drain first and passes on what it emitted. And a node on a building footprint surcharged onto a cell `swe2d._update_depth` zeroes and skips before tallying, destroying 5,185.4 m3 while the surface's own audit still closed; `compute_exchange` now refuses the exchange there, as it already did for a node with no cell.
- Alternatives: accepting the new number as an honest denominator and restating the budget (the decomposition showed invented and destroyed water, not a smaller divisor); capping the coupling's surcharge at the drain's storage curve (the cap that binds is the node's whole outflow, pipes included, which the coupling cannot see); moving the surcharge onto the nearest open cell (a node under a building has no street to surcharge onto; that is a city-pipeline question).
- Consequence: 08:40 goes from 0.204 % to 0.00049-0.00050 % (TWIN 4.88e-06; the verifier 5.02e-06 twice, bit-identical, after Sky's mass correction changed the rain). The 14.5-16 m3 left is the inlet gap - the surface offers min(wanted, available) per CFL sub-step while the network takes what it was offered - and is reported, not closed. The 2026-09-23 runs were overwritten on disk by the 2026-09-13 copies partway through the work, so the verifier compared against those and measured the improvement as 144x rather than 418x; both baselines are real, and the re-bake below supersedes both. Depths change, so every earlier run is stale.
- Date: 2026-09-24

## ADR-0073 Caches keyed on id() handed one drain network another's outfalls

- Context: `drain1d._SCRATCH` and `coupling._NODE_CACHE` were keyed on `id(solver)` and `id(network)` and validated by node and edge count. CPython reuses an address as soon as its object is collected, and two same-sized networks are exactly what the fixtures are. After the ADR-0072 fix disturbed the heap, `test_hot_start`'s tide-locked fixture reported the free-outfall fixture's numbers to the digit (outfall +2.0 m3 where the tide pushes 5.2 m3 back up the trunk). Nothing raised, and it passed in a bare process: whether it fires depends on the allocator.
- Decision: all three caches (`_SCRATCH`, `_NODE_CACHE` and the new `_SCATTER_CACHE`) hold a strong reference to their key object and compare it with `is`, one entry each, cleared on a miss. `TestScratchCacheIdentity` forces the collision with a `gc.collect()` between two same-sized networks rather than waiting for the allocator.
- Alternatives: a `WeakKeyDictionary` (`DrainSolver` and `DrainNetwork` are frozen slotted dataclasses without `weakref_slot`); a content hash (dearer than the lookup it guards on 50,110-element arrays); storing the scratch on the solver (frozen).
- Consequence: one solver and one network stay alive per cache while a run is in flight. The size check was itself the fix for an earlier case of this bug (ADR-0035's out-of-bounds scatter), which is the lesson: it caught the sizes that differ and left the sizes that match. Any earlier process that prepared two same-sized networks could have stepped the wrong outfalls and said nothing.
- Date: 2026-09-24

## ADR-0074 The drain scatter is coloured and 3.5x faster, and the Twin still misses 8 s

- Context: ADR-0035 named P4.6's remaining work: "the remaining factor of seven needs the edge scatter parallelised by colouring". The two edge passes in `drain_kernel` scatter-add onto nodes, so a plain `prange` over edges races.
- Decision: colour the edge graph greedily so no two edges in a colour share a node, run the large colours under `prange` and the tail serially. Colouring rather than atomics or per-thread reduction, because inside a colour every node is written by one edge and the addition order is a function of the network alone - a per-thread reduction would make it depend on the thread count, which rule 8 forbids of a bake. Opt-in, and only the runner asks for it.
- Alternatives: permuting the edge arrays into colour order for locality (needs a permutation back for `flow_out` every step); capping Numba's global thread count at the physical cores (a process-wide side effect on the surface solver, unmeasured).
- Consequence: Mumbai's 49,770 edges take 22 colours, three carrying 95.5 % of them; 0.12 s once. The drain step is 3.49x faster at 8 threads and 3.63x at 4 (7.68 to 2.20 ms per step, interleaved best of four), byte-identical between 1 and 8 threads. **The 8 s budget is still missed**: the verifier measured a full 3-hour coupled run at 36.07 s (drain 18.5, coupling 7.0, surface 5.5 s) with 12 python processes, and the same code has measured 86.9 s and 226 s under load. The drain is 42 % of the run, so Amdahl caps what colouring could ever give at 1.72x; ADR-0035's factor of seven was arithmetic against a stage that is no longer most of the run. What remains is the surface solver and the 2,160 coupling calls.
- Date: 2026-09-24

## ADR-0075 Sky's members run in worker processes, and the lead times pySTEPS interpolates get their mass back

- Context: section 11.1 budgets Sky at 5 s and it measured 6.32 s warm; its third test (ensemble mean within 15 % of persistence at lead 0) shipped `xfail(strict=True)` at a median 21.7 % low (ADR-0040). The nowcast is 322 ms per member with an 8 ms fixed cost, and 57 % of it is `scipy.ndimage.geometric_transform` inside pySTEPS' advection, which holds the GIL - so the thread-based `num_workers` tried in ADR-0040 never could have helped. And the lead-0 bias is two defects stacked: pySTEPS reaches a fractional lead by blending the bracketing states linearly in dBR, a geometric mean in rain, and VARUNA asks for 5-minute leads from 10-minute radar, so every odd lead is low by construction - a saw-tooth 10-13 points deep across three storms.
- Decision: split the members across four worker processes (`varuna_sky.ensemble_pool`), each group seeded by replaying pySTEPS' own per-member seed chain so the split cube equals the single call element for element; never wait for the pool inside a cycle, and have `make bake` warm it once before its loop. Rescale each interpolated lead, per member, to the domain total its two whole-step neighbours imply (`correct_interpolated_mass`).
- Alternatives: whole input steps only, advecting the half steps ourselves in rain space (needs pySTEPS' accumulated displacement, which is not exposed); linear interpolation in rain with no advection (conserves mass and draws one cell as two half-weight blobs 2.4 km apart); `probmatching='mean'` (ADR-0040 rejected it for +52 % on the 99.9th percentile).
- Consequence: steady state 2.30 s against 5 s (the verifier, 12 python processes; the chunk measured 4.75-5.61 s under heavier load), and 8.78 s for the first cycle in a process while the pool warms. Lead-0 bias goes from a median 22.6 % to 5.4 % over five storms, at a cost of +7.3 to +25.1 % on the lead-0 99.9th percentile, every corrected value still below the analysis' own tail. **That pass is close to true by construction**, and is said so here: lead 0 is a half step, its lower neighbour is the analysis, and the analysis is what the test compares against, so while the whole-step deficit sits at 10.9 % the corrected lead 0 lands near 5.4 %. The whole-step deficit - 6-13 % at ten minutes and past 30 % by two hours - is the STEPS cascade's own, untouched, and has its own test. VARUNA is now pinned to a pySTEPS internal (the seed chain), which a test asserts directly. On Windows the pool re-imports `__main__` in every worker, so a script that calls Sky without an `if __name__ == "__main__"` guard runs its body once per worker.
- Date: 2026-09-24

## ADR-0076 The street ensemble is 50 members over a covered Sky axis, its width is the weather's, and Pulse now runs before Flash

- Context: section 11.7 asks for 50 members from 20 Sky members crossed with blockage draws from the Pulse posterior "with replacement", plus Monte Carlo noise on the emulator's storage coefficient `k_u`. The cycle shipped 20 members at a uniform zero blockage, because the shipped fit's `beta_ref` is zero everywhere. Measured through `varuna_flash.ensemble.build_members` on the 2 July 08:40 cycle (7,646 wet segments): band 16.34 cm at 50 members, 9.87 cm from the weather alone, 8.87 cm from the parameters alone; switching the blockage draw off moves the band by -0.14 cm at 50 members and +0.03 cm at 60, either side of zero. Inside the parameter axis `k_u` is 8.35 cm and blockage 0.65 cm. Sampling the Sky axis with replacement moved the band 29.4 % across five seeds.
- Decision: ship 50 members, cover the Sky axis exhaustively (`m % 20`) and draw only the parameters, and carry a per-cycle `SpreadReport` that prices each axis on that cycle's own numbers, with a note saying the width is the weather's and the storage coefficient's, not Pulse's. The cycle now runs Pulse before Flash, because the members draw their blockage from this cycle's posterior; Pulse reads only the network, the bundle's feeds and the clock, so nothing it needs moved. The run id is settled before Pulse by checking every reason Flash could refuse first (`_flash_plan`).
- Alternatives: keeping 20 and calling P7.6 unbuildable (the `k_u` axis is real); dropping the blockage draw (11.7 asks for it, it is cheap, and a measured zero is worth more on screen than a missing axis); with-replacement sampling as worded (29.4 % of the reported spread would be the generator's choice); 60 members, which removes the 2.0 % allocation artefact of 50 over 20 (11.7 says 50, and the notes print the artefact); passing the previous cycle's posterior (it would be quietly stale).
- Consequence: every baked run carries `ensemble_n` 50 and a sentence pricing its axes. The members still differ only in the amplitude of the AOI-mean rain, because that is the only per-member rain a cycle keeps, and the note says so. Section 11.11's order is changed to Sky, Twin, Pulse, Flash, products; the Flash stage is milliseconds, so the parallelism 11.11 describes costs nothing to give up. The band also reaches the two places section 7.2 draws it: `run.json` carries `aoi_depth_band` - quantiles across members of the mean street depth, not the mean of per-street quantiles, because the spreads of different streets do not happen together - for the time bar, and each hotspot carries its Twin level with its registered streets' member spread for the drawer's fan chart. Both had promised a band that nothing drew.
- Date: 2026-09-26

## ADR-0077 The physics check runs the Twin twice on a 990 m window and compares changes, not levels

- Context: section 7.7 wants "Emulator vs physics: max difference N cm at X" inside section 14's 10 s, and a full-AOI Mumbai Twin run is 36-114 s. The endpoint was a 501 stub and both what-if screens said so.
- Decision: `POST /v1/whatif/physics-check` crops the city to 33 x 33 cells around the run's worst hotspot, turns every pipe that leaves the window into a free outfall, runs the coupled Twin at the run's posterior blockage for the baseline and the scenario, and reports `|emulator change - Twin change|` per hotspot inside the window, naming those outside it. The lab and the console drawer call it through one hook and print the bar, the per-junction changes, the window, the mass balance and the check's own time against 10 s.
- Alternatives: comparing levels (the same storm peaks at 87 cm on a 990 m window and 123 cm on a 2,430 m one, and the run's product reads 12.0 cm where the crop's centre reads 15.4 cm); dropping edges that leave the window (it dams the junction); keeping the 501.
- Consequence: 2.4-4.7 s warm (p95 4.09-7.37 s over five calls), 19.4-22.6 s on a process's first call, so the budget is met warm and the API has to be started before the judges arrive. On the 08:40 cycle at rain +30 % the disagreement at Bandra Talao was 5.07 cm against a 5 cm tolerance, so the bar reads "Outside tolerance", as it should. One junction's window at a time; no level is published from a crop; the crop's mass balance is 0.0 at the posterior blockage and 1.7e-02 at the city prior. A tide offset is refused here too, because there is no emulator answer to check.
- Date: 2026-09-26

## ADR-0078 A 5 m nest imposes the parent's depth at its edge, and is quoted for the pattern, never for a junction

- Context: P4.8 asks for 5 m nests at Hindmata and King's Circle with boundary heads from the 30 m run. The nest's ground is the parent's DEM resampled bicubically (section 10.1), and bicubic is not monotone across the +5 m building burn, so on the Hindmata ring the two grids disagree about the same ground by up to 2.89 m. Imposing the parent's water level there put 17,994,252 m3 across the boundary of a 1.04 km2 window against 1,123 m3 of rain on the 06:00 cycle, and drove it to 6.58 m where the parent peaked at 2.4 cm, with the mass balance passing at 5.8e-15 because the water really did cross the boundary.
- Decision: `varuna_twin.nests` snaps the window to whole parent cells (34 x 34 = 1,020 m, 204 x 204 at 5 m), runs the same `swe2d.SurfaceStepper` rather than a fork, and imposes the parent's **depth** on a one-cell ring, ramped between the parent's 5-minute outputs; `NestRun.ring_ground_gap_m` reports what that costs in surface height. `city.load_nests` binds each nest to exactly one verified, sourced register point or refuses. A non-finite CFL step now raises instead of silently ending the run.
- Alternatives: imposing level (measured above); a bilinear DEM (monotone, no ring gap, but section 10.1 says bicubic and the two disagree at the junction); a nest with its own 5 m drain graph (none exists).
- Consequence: the same run reads 291 m3 in and 237 m3 out with a 0.43 m peak, and the nests conserve to 2e-15 and 4e-15. **A nest may not be quoted as a better depth at a named junction**: under a 50 mm/h design storm the 30 m cell at Hindmata holds 25.1 cm, the bicubic nest 47.1 cm and a bilinear one 19.5 cm, so the sign of the difference is the interpolant's. What survives is the aggregate, 6.1 cm mean absolute difference and under 1 cm of bias over the window: the finer grid redistributes water into hollows rather than adding any. One nest takes 9.2-11.9 s for 3 hours, 1.7x the whole 30 m city, so a nest is on demand and not a per-cycle product; nothing calls `run_nest` yet and there is no raster writer, so a nest cannot reach the console.
- Date: 2026-09-24

## ADR-0079 PySWMM dynamic wave is exported and adapted, and it does not replace drain1d

- Context: section 11.4 names PySWMM dynamic wave as the P1 upgrade behind the same interface, and section 16 promises "when a SWMM model arrives we import it". P4.9 asked for the adapter, the `.inp` export and the gap between the two engines as a number.
- Decision: `varuna_twin.swmm` writes the inferred graph as a SWMM 5 `.inp` (blockage as roughness, `n / (1 - beta)`, capacity-preserving; circular or closed-rectangular sections recovered from area and crown depth) and drives the engine through `drain1d.simulate`'s call shape. `drain1d` stays the P0 solver everywhere. `pyswmm>=2.1` is a dev dependency, not a runtime one.
- Alternatives: exporting manholes as storage nodes to match `drain1d`'s storage (a SWMM modeller expects junctions, and the difference is a finding); a large `SurDepth` so SWMM pressurises rather than spills (measured: under EXTRAN 100 m of headroom put the head at 105 m over a Mumbai street and it flooded anyway; under SLOT 47.5 m and a 44.3 % continuity error).
- Consequence: the reduced model's cost is a number now. Flows agree to 2.7e-16 m3/s, but on a 600 mm pipe at 0.5 % carrying 10 L/s `drain1d` needs 1.38 cm of depth where SWMM needs 6.28 cm, 4.5x, traceable to its full-bore hydraulic radius, and a hand calculation of Manning's normal depth agrees with SWMM to 0.25 %. SWMM keeps its water in the conduits and `drain1d` in the manholes (45.9x). And the upgrade **misses section 11.3's own budget**: -1.901 % on a cold 30-minute run where `drain1d` reads 0.0000 %, a fixed 0.342 m3 lost while filling that no routing setting moves. Run beside section 11.5's coupling, SWMM's own spill counts the same water twice; that is measured and warned on, not solved. The whole Mumbai graph exports in 0.62 s to 12.4 MB, byte-identical on re-export, and counts 18,994 uphill conduits, ADR-0048's figure by a different route.
- Date: 2026-09-24

## ADR-0080 The per-screen design QA is a test, and a third party's console errors are attributed, not excused

- Context: section 6.11 ends every screen's build with an eight-line checklist and P10.2 wants a screenshot per route. Neither had been done screen by screen: three routes had never been photographed, and only four screens were held to section 14's zero console errors. Four of 6.11's lines are machine-checkable and four are judgements.
- Decision: `tests/e2e/design-qa.spec.ts` walks all seventeen routes of section 3.4, writes `docs/screens/<route>.png`, and asserts no horizontal scroll at 1366 x 768, 1440 x 900 and 2560 x 1440 (390 x 844 for the phone screens), a visible focus ring on the first tab stop, no placeholder copy, and no console errors. The four judgement lines are named in a `HUMAN_EYE` constant and annotated on every run. A screen may declare one third-party script, and its errors are attributed by source URL or stack, never by message shape.
- Alternatives: an `xfail`-style assertion that the known error still happens (Google's loader errored on two runs of three, so it flaked); byte-comparing screenshots (fails on hinting and every legitimate redesign); proxies for the judgement lines (the checklist would read green while nothing had been looked at).
- Consequence: 34 tests, 17 routes, about 10 minutes. The first pass found six real defects, all fixed: the replay log keyed rows on cycle time (29 duplicate-key warnings, rows silently dropped); the console's replay panel passed a hard-wired empty array; the log's stages column printed one constant on every row; the cycle picker drew 17 chips for 8 cycles; the landing page's first tab stop was deck.gl's unnamed events root with `outline: none`; and `/dashboard` blamed a referrer restriction for what was `InvalidKeyMapError`. The four judgement lines are still a person's job over the seventeen screenshots, and now say so every run.
- Date: 2026-09-24

## ADR-0081 Every section 14 browser and API budget has a verdict from a repeatable run

- Context: P10.4 is a performance pass, and most of section 14's budgets had been measured once on a dev build or never.
- Decision: `tests/e2e/performance.spec.ts` measures a production build served from `.next-perf` beside a running `next dev` (`VARUNA_DIST_DIR`), prints sixteen budgets in one table, and an opt-in GPU block decides the ones a headless browser cannot. A boot probe separates the run stamp arriving in the DOM from the main thread being free, and a bundle-weight test reports JavaScript per route.
- Alternatives: measuring on `next dev` (it invents stalls the product does not have); a headless browser for frame rate (SwiftShader measured 49.3-55.8 fps where the Iris Xe measured 57.0-60.1).
- Consequence: met - landing LCP 160-212 ms, CLS 0, map 57.0-60.1 fps at 1440 x 900 on the Iris Xe, tab memory 676-959 MB over a full replay against 1 GB, 0 requests during a scrub, route p95 64-82 ms, what-if 301-581 ms, physics check 4.09-7.37 s. Missed - the console's first render (run stamp in the DOM at 1.0-1.8 s, then the main thread blocked 3.6-5.7 s; the obvious suspects were measured and cleared, and 530 KB of Google's 3D tile loader and 382 KB of charts behind closed panels are the named target), the scrub (the key handler alone 19-21 ms against 16), and `GET /v1/nowcast/segments` (507-553 ms p95 gzipped, because the handler serves the whole 3.5 MB AOI and ignores its documented `bbox`). The live cycle and Chennai onboarding were not measured in the pass because both write where other work was reading.
- Date: 2026-09-24

## ADR-0082 The demo-run seeder deleted fresh bakes whose alerts differed from the shipped set

- Context: on 2026-09-24 a complete re-bake of the seven demo cycles vanished from `data/runs` twenty minutes after it was written, replaced by the 2026-09-13 copies; the verification pass then measured the old runs and concluded the re-bake's numbers were not reproducible. On 2026-09-26 it happened again, 23 seconds after an API test started, and a `.seeded` marker appeared in the overwritten runs. `seed_demo_runs` runs on every API start and treats an unmarked run that "lacks" a file the shipped copy has as an old seeded copy, which it deletes and replaces. A shipped run's `alerts/` folder is named by the alerts its own cycle raised, so a fresh bake of the same cycle always lacks some of them.
- Decision: a run holding `segment_forecast.parquet` - the product of record a bake writes and the shipped set deliberately omits - is this machine's work and is never replaced or pruned, whatever else it lacks. API tests no longer seed at all (`VARUNA_SEED_DEMO_RUNS=0` from an autouse fixture), because the app seeds whatever `VARUNA_DATA_DIR` points at, fixtures run in signature order, and a `client` requested before a temporary data directory started the app against the developer's own `data/runs`.
- Alternatives: comparing only a fixed list of product names (the next product added to the shipped set would reopen it); never replacing an unmarked run (breaks the migration ADR-0024's markers exist for, where an old seeded copy lacks a product the image now carries); moving every API test onto a temporary data directory (right in principle, but several tests read the real baked runs and would need fixtures of their own).
- Consequence: two tests pin it - a fresh bake with different alert documents survives a seed, and a local bake is never pruned even with a stray marker. The earlier "the wave brief's numbers are wrong" finding is withdrawn in `docs/QA.md`: the 2026-09-23 figures were real and are in the bake log; the runs that carried them were deleted by this. Anything that starts the API against a working tree - `make demo`, a test, a Playwright run - was able to do it.
- Date: 2026-09-26
